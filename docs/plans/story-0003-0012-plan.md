# Implementation Plan -- STORY-0003-0012: x-dev-implement -- Red-Green-Refactor Implementation

**Status:** DRAFT
**Date:** 2026-03-15
**Story:** story-0003-0012
**Blocked by:** story-0003-0006 (DONE), story-0003-0007 (DONE), story-0003-0008 (DONE)
**Blocks:** story-0003-0014

---

## 1. Affected Files and Components

### 1.1 Template Source Files (Source of Truth -- RULE-002)

| # | File | Action | Description |
|---|------|--------|-------------|
| 1 | `resources/skills-templates/core/x-dev-implement/SKILL.md` | **MODIFY** | Claude/Agents skill template. Restructure Steps 1-4 for TDD workflow with Double-Loop, TPP ordering, and atomic commits |
| 2 | `resources/github-skills-templates/dev/x-dev-implement.md` | **MODIFY** | GitHub Copilot skill template. Mirror structural changes with GitHub-specific formatting (RULE-001) |

### 1.2 Golden Files -- `.claude/skills/x-dev-implement/SKILL.md` (8 profiles)

| # | Profile | Path |
|---|---------|------|
| 3 | go-gin | `tests/golden/go-gin/.claude/skills/x-dev-implement/SKILL.md` |
| 4 | java-quarkus | `tests/golden/java-quarkus/.claude/skills/x-dev-implement/SKILL.md` |
| 5 | java-spring | `tests/golden/java-spring/.claude/skills/x-dev-implement/SKILL.md` |
| 6 | kotlin-ktor | `tests/golden/kotlin-ktor/.claude/skills/x-dev-implement/SKILL.md` |
| 7 | python-click-cli | `tests/golden/python-click-cli/.claude/skills/x-dev-implement/SKILL.md` |
| 8 | python-fastapi | `tests/golden/python-fastapi/.claude/skills/x-dev-implement/SKILL.md` |
| 9 | rust-axum | `tests/golden/rust-axum/.claude/skills/x-dev-implement/SKILL.md` |
| 10 | typescript-nestjs | `tests/golden/typescript-nestjs/.claude/skills/x-dev-implement/SKILL.md` |

### 1.3 Golden Files -- `.agents/skills/x-dev-implement/SKILL.md` (8 profiles)

These are byte-for-byte identical to the `.claude/` copies. Generated by `CodexSkillsAssembler` which mirrors `.claude/skills/` to `.agents/skills/`.

| # | Profile | Path |
|---|---------|------|
| 11 | go-gin | `tests/golden/go-gin/.agents/skills/x-dev-implement/SKILL.md` |
| 12 | java-quarkus | `tests/golden/java-quarkus/.agents/skills/x-dev-implement/SKILL.md` |
| 13 | java-spring | `tests/golden/java-spring/.agents/skills/x-dev-implement/SKILL.md` |
| 14 | kotlin-ktor | `tests/golden/kotlin-ktor/.agents/skills/x-dev-implement/SKILL.md` |
| 15 | python-click-cli | `tests/golden/python-click-cli/.agents/skills/x-dev-implement/SKILL.md` |
| 16 | python-fastapi | `tests/golden/python-fastapi/.agents/skills/x-dev-implement/SKILL.md` |
| 17 | rust-axum | `tests/golden/rust-axum/.agents/skills/x-dev-implement/SKILL.md` |
| 18 | typescript-nestjs | `tests/golden/typescript-nestjs/.agents/skills/x-dev-implement/SKILL.md` |

### 1.4 Golden Files -- `.github/skills/x-dev-implement/SKILL.md` (8 profiles)

These are produced by `GithubSkillsAssembler` from `resources/github-skills-templates/dev/x-dev-implement.md`. The template engine runs `replacePlaceholders()` but only resolves single-brace `{placeholder}` syntax. The `{{DOUBLE_BRACE}}` placeholders (e.g., `{{COMPILE_COMMAND}}`, `{{LANGUAGE}}`) remain unresolved intentionally -- they are runtime placeholders for the AI agent.

| # | Profile | Path |
|---|---------|------|
| 19 | go-gin | `tests/golden/go-gin/.github/skills/x-dev-implement/SKILL.md` |
| 20 | java-quarkus | `tests/golden/java-quarkus/.github/skills/x-dev-implement/SKILL.md` |
| 21 | java-spring | `tests/golden/java-spring/.github/skills/x-dev-implement/SKILL.md` |
| 22 | kotlin-ktor | `tests/golden/kotlin-ktor/.github/skills/x-dev-implement/SKILL.md` |
| 23 | python-click-cli | `tests/golden/python-click-cli/.github/skills/x-dev-implement/SKILL.md` |
| 24 | python-fastapi | `tests/golden/python-fastapi/.github/skills/x-dev-implement/SKILL.md` |
| 25 | rust-axum | `tests/golden/rust-axum/.github/skills/x-dev-implement/SKILL.md` |
| 26 | typescript-nestjs | `tests/golden/typescript-nestjs/.github/skills/x-dev-implement/SKILL.md` |

### 1.5 Files NOT Modified

| File | Reason |
|------|--------|
| `src/assembler/skills-assembler.ts` | No change -- copies core skill templates as-is |
| `src/assembler/codex-skills-assembler.ts` | No change -- mirrors `.claude/skills/` to `.agents/skills/` |
| `src/assembler/github-skills-assembler.ts` | No change -- reads template, applies placeholder resolution |
| `src/template-engine.ts` | No change -- placeholder resolution logic unaffected |
| `resources/skills-templates/core/x-test-plan/SKILL.md` | No change -- already restructured for TDD (story-0003-0007) |
| `resources/agents-templates/core/typescript-developer.md` | No change -- already has TDD workflow (story-0003-0006) |
| `tests/node/integration/byte-for-byte.test.ts` | No change -- existing parity test validates golden files automatically |

### 1.6 Total File Count

- 2 template source files modified
- 24 golden files updated (8 profiles x 3 targets: `.claude/`, `.agents/`, `.github/`)
- **Total: 26 files**

---

## 2. Changes per File (Detailed)

### 2.1 Claude/Agents Template: `resources/skills-templates/core/x-dev-implement/SKILL.md`

This is the primary template. The `.claude/` golden files are direct copies and `.agents/` golden files are byte-for-byte mirrors.

#### 2.1.1 YAML Frontmatter (MODIFIED)

**BEFORE:**
```yaml
description: "Implements a feature/story following project conventions. Delegates preparation to a subagent that reads architecture and coding KPs, then implements layer-by-layer with intermediate compilation checks."
```

**AFTER:**
```yaml
description: "Implements a feature/story using TDD (Red-Green-Refactor) workflow. Delegates preparation to a subagent that reads architecture, coding, and test plan KPs, then implements test-first with Double-Loop TDD, layer-by-layer with compile checks after each cycle."
```

#### 2.1.2 Execution Flow Overview (MODIFIED)

**BEFORE:**
```
1. PREPARE + UNDERSTAND  -> Subagent reads KPs, produces implementation plan
2. IMPLEMENT             -> Orchestrator writes code layer-by-layer (inline)
3. TEST + VALIDATE       -> Orchestrator runs tests, checks coverage (inline)
4. COMMIT                -> Orchestrator commits following conventions (inline)
```

**AFTER:**
```
1. PREPARE + UNDERSTAND  -> Subagent reads KPs + test plan, produces TDD implementation plan
2. TDD LOOP              -> For each scenario (TPP order): RED -> GREEN -> REFACTOR -> compile check
3. VALIDATE              -> Coverage thresholds, all acceptance tests GREEN (inline)
4. COMMIT                -> Atomic TDD commits: one per Red-Green-Refactor cycle (inline)
```

#### 2.1.3 Step 1: Prepare + Understand (MODIFIED -- subagent prompt extended)

The subagent prompt is extended, NOT replaced. The existing Steps 1-4 within the subagent are preserved. Two additions:

1. **New Step between current Steps 1 and 2 (becomes Step 2, pushing others down):** Read the test plan.

```markdown
> **Step 2 — Read test plan (MANDATORY):**
> - Look for test plan at `docs/stories/epic-XXXX/plans/tests-story-XXXX-YYYY.md`
> - Extract: acceptance tests (AT-N), unit tests in TPP order (UT-N), integration tests (IT-N)
> - Identify the outer loop (acceptance tests that start RED)
> - Identify the inner loop order (unit tests in TPP sequence)
> - If NO test plan found: emit WARNING and suggest running `/x-test-plan` first, then continue with fallback mode (implement without strict TPP ordering)
```

2. **Updated Step 4 (now Step 5) -- Produce implementation plan:** Add TDD-specific items.

```markdown
> **Step 5 — Produce implementation plan:**
> 1. Layer-by-layer implementation order (from layer-templates)
> 2. For each layer: classes to create/modify, package location, key patterns
> 3. TDD cycle mapping: which UT-N scenarios apply to which layer
> 4. Acceptance test identification: AT-N entries that validate the feature end-to-end
> 5. Key conventions to follow (naming, immutability, injection style)
> 6. Dependencies to verify before starting
```

**Fallback warning** (RULE-003 backward compatibility):

```markdown
> **Fallback Mode (no test plan):**
> If no test plan file exists, log:
> `⚠ WARNING: No test plan found. Run /x-test-plan first for optimal TDD workflow. Proceeding with implementation-first approach.`
> In fallback mode, Step 2 reverts to the layer-by-layer implementation without strict TPP ordering. Tests are written alongside code (test-with) rather than test-first.
```

#### 2.1.4 Step 2: TDD Loop (NEW -- replaces old "Implement")

The old Step 2 ("Implement -- Orchestrator Inline") is **replaced entirely** by the TDD Loop.

```markdown
## Step 2: TDD Loop (Orchestrator -- Inline)

Using the TDD implementation plan returned by the subagent, execute Red-Green-Refactor cycles.

**Layer order is preserved WITHIN each cycle.** When a UT-N test touches domain, that cycle implements domain first, then adapters. Dependencies point inward (RULE-009).

### 2.0 Write Acceptance Test First (Double-Loop -- Outer Loop)

Before any unit test cycle, write the acceptance test(s) from the test plan (AT-N entries):

1. Write the acceptance test (integration/API/E2E depending on scope)
2. Run it -- it MUST be RED (failing)
3. This acceptance test stays RED throughout the inner loop
4. It turns GREEN only after all related unit test cycles complete

```bash
{{TEST_COMMAND}} -- [acceptance-test-file]
# Expected: FAIL (RED)
```

### 2.1 Inner Loop: Red-Green-Refactor per Unit Test (TPP Order)

For each UT-N in the test plan (strict TPP order: degenerate first, edge cases last):

#### RED -- Write the Failing Test
1. Write the unit test for UT-N (test name follows `[method]_[scenario]_[expected]`)
2. Run it -- it MUST fail
3. If the test passes without new code, the test is wrong or the scenario is already covered

```bash
{{TEST_COMMAND}} -- [test-file]
# Expected: FAIL (RED)
```

#### GREEN -- Implement the Minimum
1. Write the MINIMUM production code to make UT-N pass
2. Respect layer order: domain -> ports -> adapters -> application -> inbound
3. Do NOT add code beyond what the test requires
4. Run the test -- it MUST pass
5. Run ALL previous tests -- they MUST still pass

```bash
{{TEST_COMMAND}}
# Expected: ALL PASS (GREEN)
```

#### REFACTOR -- Improve Design
1. Look for: extract method (> 25 lines), duplicate code, unclear naming
2. Refactoring NEVER adds behavior -- if behavior changes, write a new failing test first
3. Run all tests after refactoring -- they MUST still pass

```bash
{{TEST_COMMAND}}
# Expected: ALL PASS (still GREEN)
```

#### Compile Check
After each complete cycle (Red-Green-Refactor):

```bash
{{COMPILE_COMMAND}}
# Expected: zero errors, zero warnings
```

### 2.2 Cycle Completion

After all UT-N cycles for a given acceptance test (AT-N) are complete:
1. Run the acceptance test -- it should now be GREEN
2. If still RED, identify missing unit test cycles and add them
3. Proceed to the next AT-N

### 2.3 Code Conventions (from subagent's plan)

- Named constants (never magic numbers/strings)
- Methods <= 25 lines, classes <= 250 lines
- Self-documenting code (comments only for "why")
- Never return null -- use Optional/empty types
- Constructor/initializer injection
- Immutable DTOs, value objects, events

### 2.4 Fallback Mode (No Test Plan)

When operating in fallback mode (no test plan available):
- Implement layer-by-layer following the old approach
- Write tests alongside code (test-with) rather than test-first
- Still run compile check after each layer: `{{COMPILE_COMMAND}}`
- Log a reminder: `⚠ Consider running /x-test-plan for future implementations`
```

#### 2.1.5 Step 3: Validate (MODIFIED -- was "Test + Validate")

**BEFORE:** Generic test validation with "ideally test-first" language.

**AFTER:** TDD-specific validation confirming the Double-Loop is complete.

```markdown
## Step 3: Validate (Orchestrator -- Inline)

All TDD cycles are complete. Run final validation:

```bash
{{TEST_COMMAND}}
{{COVERAGE_COMMAND}}
```

**Definition of Done:**

| Criterion | Verification |
|-----------|-------------|
| All acceptance tests (AT-N) GREEN | Run full test suite |
| All unit tests (UT-N) GREEN | Run full test suite |
| Line coverage >= 95% | Coverage report |
| Branch coverage >= 90% | Coverage report |
| Code compiles cleanly | `{{COMPILE_COMMAND}}` with no warnings |
| All tests pass | `{{TEST_COMMAND}}` |
| Tests written BEFORE implementation | Verify test-first pattern in each cycle |
| Refactoring evaluated per cycle | Each cycle has explicit refactor step (even if noop) |
| Thread-safe (if applicable) | No mutable static state |
```

#### 2.1.6 Step 4: Commit (MODIFIED -- atomic TDD commits)

**BEFORE:** Generic atomic commits per layer/feature.

**AFTER:** Atomic commits per TDD cycle.

```markdown
## Step 4: Commit (Orchestrator -- Inline)

Make atomic commits per TDD cycle. Each commit contains the test AND its implementation from one Red-Green-Refactor cycle:

```bash
# Per TDD cycle (UT-N):
git add [test-file-for-UT-N]
git add [implementation-files-for-UT-N]
git commit -m "feat(scope): add [behavior] (UT-N)

- RED: [test description]
- GREEN: [minimum implementation]
- REFACTOR: [what was improved, or 'noop']"
```

For acceptance tests, commit after they turn GREEN:

```bash
git add [acceptance-test-file]
git commit -m "test(scope): add acceptance test for [AT-N scenario]"
```

**Commit ordering must reflect TDD progression:**
1. Acceptance test commit (RED) -- first
2. Unit test + implementation commits (UT-1, UT-2, ...) -- in TPP order
3. Final commit when AT turns GREEN (if AT needed updates)
```

#### 2.1.7 Integration Notes (MODIFIED)

Add reference to x-test-plan as prerequisite:

```markdown
## Integration Notes

- **Prerequisite:** Run `/x-test-plan` first to generate the test plan with Double-Loop + TPP ordering
- For the full lifecycle with reviews, use `x-dev-lifecycle` instead
- Invokes patterns from `x-test-run` and `x-git-push` skills
- Works with any {{FRAMEWORK}} project following layered/hexagonal architecture
- The developer agent (typescript-developer) already includes TDD workflow rules (story-0003-0006)
```

### 2.2 GitHub Template: `resources/github-skills-templates/dev/x-dev-implement.md`

The GitHub copy is a condensed version that mirrors the Claude copy's structure. Changes must maintain RULE-001 dual copy consistency.

#### 2.2.1 YAML Frontmatter (MODIFIED)

```yaml
description: >
  Implements a feature/story using TDD (Red-Green-Refactor) workflow. Delegates
  preparation to a subagent that reads architecture, coding, and test plan knowledge
  packs, then implements test-first with Double-Loop TDD and compile checks after
  each cycle. Use for quick implementations, single class changes, or coding without
  the full review phases.
```

#### 2.2.2 Execution Flow (MODIFIED)

Same as Claude copy section 2.1.2.

#### 2.2.3 Step 1: Subagent Prompt (MODIFIED)

Mirror Claude copy changes with GitHub-specific paths:
- Add Step 2 for test plan reading (using `.github/` paths)
- Add fallback warning
- Reference `.github/skills/` paths instead of `skills/`

#### 2.2.4 Step 2: TDD Loop (NEW -- replaces old Implement)

Condensed version of Claude copy's TDD Loop, preserving key structure:
- 2.0 Acceptance Test First
- 2.1 Inner Loop (Red-Green-Refactor per UT)
- 2.2 Cycle Completion
- 2.3 Code Conventions
- 2.4 Fallback Mode

#### 2.2.5 Steps 3-4 (MODIFIED)

Mirror Claude copy changes for Validate and Commit steps.

#### 2.2.6 Detailed References (PRESERVED + EXTENDED)

```markdown
## Detailed References

For in-depth guidance on implementation patterns, consult:
- `.github/skills/x-dev-implement/SKILL.md`
- `.github/skills/layer-templates/SKILL.md`
- `.github/skills/coding-standards/SKILL.md`
- `.github/skills/x-test-plan/SKILL.md` -- for generating test plans with TPP ordering
```

---

## 3. New Content Structure for SKILL.md (Updated Steps)

### 3.1 Claude/Agents Copy -- Final Structure

```
YAML Frontmatter (updated description)
## Global Output Policy (PRESERVED)
# Skill: Implement Story (Orchestrator) (PRESERVED)
## When to Use This vs /x-dev-lifecycle (PRESERVED)
## Execution Flow (Orchestrator Pattern) (UPDATED -- 4 steps renamed)
## Step 1: Prepare + Understand (Subagent via Task) (MODIFIED -- test plan added)
  - Step 1: Read story (preserved)
  - Step 2: Read test plan (NEW -- mandatory with fallback)
  - Step 3: Read project conventions (was Step 2)
  - Step 4: Review existing code (was Step 3)
  - Step 5: Produce implementation plan (was Step 4 -- TDD fields added)
  - Fallback Mode warning (NEW)
  - Branch creation (preserved)
## Step 2: TDD Loop (Orchestrator -- Inline) (NEW -- replaces old Implement)
  - 2.0 Write Acceptance Test First (Double-Loop)
  - 2.1 Inner Loop: Red-Green-Refactor per UT (TPP Order)
    - RED
    - GREEN
    - REFACTOR
    - Compile Check
  - 2.2 Cycle Completion
  - 2.3 Code Conventions (moved from old Step 2)
  - 2.4 Fallback Mode (NEW)
## Step 3: Validate (Orchestrator -- Inline) (MODIFIED -- TDD-specific DoD)
## Step 4: Commit (Orchestrator -- Inline) (MODIFIED -- atomic TDD commits)
## Integration Notes (MODIFIED -- x-test-plan prerequisite)
```

### 3.2 GitHub Copy -- Final Structure

```
YAML Frontmatter (updated description)
# Skill: Implement Story (Orchestrator) (PRESERVED)
## When to Use This vs /x-dev-lifecycle (PRESERVED)
## Execution Flow (Orchestrator Pattern) (UPDATED)
## Step 1: Prepare + Understand (Subagent via Task) (MODIFIED)
## Step 2: TDD Loop (Orchestrator -- Inline) (NEW)
## Step 3: Validate (Orchestrator -- Inline) (MODIFIED)
## Step 4: Commit (Orchestrator -- Inline) (MODIFIED)
## Integration Notes (MODIFIED)
## Detailed References (PRESERVED + EXTENDED)
```

---

## 4. Golden File Impact Analysis

### 4.1 Generation Mechanism per Target

| Target | Source Template | Assembler | Placeholder Resolution |
|--------|---------------|-----------|----------------------|
| `.claude/` | `resources/skills-templates/core/x-dev-implement/SKILL.md` | `SkillsAssembler` (direct copy) | None -- `{{PLACEHOLDER}}` preserved as-is |
| `.agents/` | Mirrors `.claude/skills/x-dev-implement/SKILL.md` at build time | `CodexSkillsAssembler` (byte-for-byte copy) | None -- identical to `.claude/` |
| `.github/` | `resources/github-skills-templates/dev/x-dev-implement.md` | `GithubSkillsAssembler` (template engine) | `{single_brace}` resolved; `{{DOUBLE_BRACE}}` preserved |

### 4.2 Profile-Specific Differences

| Target | Profile-specific? | Reason |
|--------|-------------------|--------|
| `.claude/` | **No** -- all 8 profiles identical | Template has no single-brace placeholders |
| `.agents/` | **No** -- mirrors `.claude/` exactly | Byte-for-byte copy |
| `.github/` | **No** -- all 8 profiles identical | Template has no single-brace `{placeholder}` that differ per profile; only `{{DOUBLE_BRACE}}` which are preserved |

**Verification:** Compared current golden files across profiles and confirmed all 8 `.claude/` copies are identical, all 8 `.agents/` copies are identical (and match `.claude/`), and all 8 `.github/` copies are identical.

### 4.3 Update Strategy

Since all profiles produce identical files per target:

1. Modify `resources/skills-templates/core/x-dev-implement/SKILL.md`
2. Copy to all 8 `.claude/` golden paths
3. Copy to all 8 `.agents/` golden paths (identical content)
4. Modify `resources/github-skills-templates/dev/x-dev-implement.md`
5. Run pipeline to regenerate `.github/` goldens, OR manually copy since no profile-specific substitution occurs

### 4.4 Total Golden Files Impacted

- 8 profiles x 3 targets = **24 golden files**
- All updates are deterministic (no profile-specific variation)

---

## 5. Integration Test Impact

### 5.1 Existing Test Coverage

The `byte-for-byte.test.ts` integration test (`tests/node/integration/byte-for-byte.test.ts`) runs for all 8 profiles and validates:

1. Pipeline executes successfully
2. Output matches golden files byte-for-byte
3. No missing files
4. No extra files
5. Total files > 0

### 5.2 Changes Required

**None.** The existing byte-for-byte test automatically validates golden file parity. After updating the 24 golden files, the tests will pass if and only if the pipeline produces correct output.

### 5.3 Test Execution Plan

```bash
# Run byte-for-byte tests to validate golden files
npx vitest run tests/node/integration/byte-for-byte.test.ts

# Run full test suite
npm test
```

### 5.4 Expected Failures Before Golden File Update

If templates are modified but golden files are not yet updated, the byte-for-byte test will fail with `pipelineMatchesGoldenFiles` failures for all 8 profiles. This is expected and will be resolved by updating golden files.

---

## 6. Dual Copy Consistency Strategy (RULE-001)

### 6.1 Three Copy Architecture

The x-dev-implement skill exists in three forms:

| Copy | Source | Purpose |
|------|--------|---------|
| Claude (`.claude/`) | `resources/skills-templates/core/x-dev-implement/SKILL.md` | Claude Code CLI |
| Agents (`.agents/`) | Mirrors `.claude/` via `CodexSkillsAssembler` | OpenAI Codex CLI |
| GitHub (`.github/`) | `resources/github-skills-templates/dev/x-dev-implement.md` | GitHub Copilot |

### 6.2 Structural Parity

Both templates MUST have:
- Same 4-step execution flow (Prepare, TDD Loop, Validate, Commit)
- Same TDD concepts (Double-Loop, TPP ordering, Red-Green-Refactor)
- Same fallback warning when test plan is absent
- Same atomic commit pattern

### 6.3 Permitted Differences

| Aspect | Claude/Agents | GitHub |
|--------|--------------|--------|
| Frontmatter format | `allowed-tools`, `argument-hint` fields | `description` as block scalar |
| Global Output Policy | Present | Absent (GitHub handles this differently) |
| KP path references | `skills/architecture/references/...` | `.github/skills/architecture/SKILL.md` |
| Detailed References section | Absent | Present (links to `.github/skills/`) |
| Verbosity | Full detail | Slightly condensed |

### 6.4 Verification Procedure

After implementation, verify structural parity:
1. Both templates have the same 4 step names
2. Both templates reference test plan as mandatory input
3. Both templates include fallback warning
4. Both templates describe Red-Green-Refactor cycle
5. Both templates describe acceptance test first (Double-Loop)
6. Both templates describe atomic TDD commits

---

## 7. Risk Assessment

### 7.1 High Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| None identified | -- | -- | -- |

### 7.2 Medium Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Subagent prompt becomes too long | Medium | Medium | Keep test plan reading instructions concise; use bullet points not prose |
| Fallback mode ambiguity | Medium | Medium | Clearly define what "fallback" means: layer-by-layer + test-with, not test-first |
| x-dev-lifecycle consuming x-dev-implement output | Medium | Medium | x-dev-lifecycle delegates to developer agent, not this skill; verify no coupling |

### 7.3 Low Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Subagent Step numbering shift breaks something | Low | Low | Subagent steps are read by AI, not parsed programmatically |
| `{{PLACEHOLDER}}` accidentally removed | Low | High | Verify all `{{COMPILE_COMMAND}}`, `{{TEST_COMMAND}}`, etc. are preserved in both templates |
| Golden file copy error | Low | Medium | Use pipeline regeneration, not manual copy, for `.github/` goldens |
| byte-for-byte test failures after update | Expected | Low | Update all 24 golden files before running tests |
| Increased skill template line count | Low | Low | x-dev-implement is already a moderate skill; TDD loop adds ~80 lines but replaces ~20 |

### 7.4 Backward Compatibility (RULE-003)

The skill's external interface (invocation via `/x-dev-implement [STORY-ID]`) is **unchanged**. The behavioral change is:

| Aspect | Before | After |
|--------|--------|-------|
| Test plan | Optional (not read) | Mandatory (with fallback) |
| Implementation order | Layer-by-layer | TDD cycles in TPP order (within layer order) |
| Test writing | "Alongside or test-first" | Test-first (mandatory) |
| Commits | Per layer/feature | Per TDD cycle |

The fallback mode (Section 2.4) ensures backward compatibility when no test plan exists: the skill degrades to the original layer-by-layer approach with a warning.

### 7.5 Subagent Pattern Preservation (RULE-009)

- Step 1 still launches a **single** `general-purpose` subagent
- The subagent prompt is extended (new Step 2 for test plan), not replaced
- The subagent's KP reading steps are preserved
- Layer order is preserved WITHIN each TDD cycle
- No new subagents are introduced
- No parallel subagent execution is added (this skill uses a single subagent)

---

## 8. Implementation Order (Recommended)

### Phase 1: Template Changes (Claude/Agents Copy)

1. Modify `resources/skills-templates/core/x-dev-implement/SKILL.md` with all changes from Section 2.1
2. Copy the updated template to all 8 `.claude/` golden paths
3. Copy the updated template to all 8 `.agents/` golden paths

### Phase 2: Template Changes (GitHub Copy)

4. Modify `resources/github-skills-templates/dev/x-dev-implement.md` with all changes from Section 2.2
5. Regenerate `.github/` golden files via pipeline OR copy manually (since no profile-specific substitution)

### Phase 3: Verification

6. Run byte-for-byte tests: `npx vitest run tests/node/integration/byte-for-byte.test.ts`
7. Run full test suite: `npm test`
8. Verify compilation: `npx tsc --noEmit`
9. Verify coverage: `npx vitest run --coverage`

### Phase 4: Commit

10. Atomic commit with all template and golden file changes

---

## 9. Summary of Changes

| Metric | Value |
|--------|-------|
| Template files modified | 2 |
| Golden files updated | 24 (8 profiles x 3 targets) |
| Source code (TypeScript) modified | 0 |
| New files created | 0 |
| Files deleted | 0 |
| Subagent changes | Extended (new step for test plan reading), NOT replaced |
| Backward compatible | Yes (fallback mode when no test plan) |
| Test infrastructure changes | None (existing byte-for-byte tests validate) |
