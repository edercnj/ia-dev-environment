# Implementation Plan -- STORY-0003-0007: x-test-plan -- Promotion to Implementation Driver with TPP

**Status:** DRAFT
**Date:** 2026-03-15
**Story:** story-0003-0007
**Blocked by:** story-0003-0001 (Testing KP with TDD/TPP -- DONE)
**Blocks:** story-0003-0008, story-0003-0012, story-0003-0014

---

## 1. Affected Files and Components

### 1.1 Template Source Files (Source of Truth -- RULE-002)

| # | File | Action | Description |
|---|------|--------|-------------|
| 1 | `resources/skills-templates/core/x-test-plan/SKILL.md` | **MODIFY** | Claude/Agents skill template. Restructure Step 2 and Output sections for Double-Loop + TPP ordering |
| 2 | `resources/github-skills-templates/testing/x-test-plan.md` | **MODIFY** | GitHub Copilot skill template. Mirror structural changes with GitHub-specific formatting (RULE-001) |

### 1.2 Golden Files -- `.claude/skills/x-test-plan/SKILL.md` (8 profiles)

| # | Profile | Path |
|---|---------|------|
| 3 | go-gin | `tests/golden/go-gin/.claude/skills/x-test-plan/SKILL.md` |
| 4 | java-quarkus | `tests/golden/java-quarkus/.claude/skills/x-test-plan/SKILL.md` |
| 5 | java-spring | `tests/golden/java-spring/.claude/skills/x-test-plan/SKILL.md` |
| 6 | kotlin-ktor | `tests/golden/kotlin-ktor/.claude/skills/x-test-plan/SKILL.md` |
| 7 | python-click-cli | `tests/golden/python-click-cli/.claude/skills/x-test-plan/SKILL.md` |
| 8 | python-fastapi | `tests/golden/python-fastapi/.claude/skills/x-test-plan/SKILL.md` |
| 9 | rust-axum | `tests/golden/rust-axum/.claude/skills/x-test-plan/SKILL.md` |
| 10 | typescript-nestjs | `tests/golden/typescript-nestjs/.claude/skills/x-test-plan/SKILL.md` |

### 1.3 Golden Files -- `.agents/skills/x-test-plan/SKILL.md` (8 profiles)

These are byte-for-byte identical to the `.claude/` copies. Generated by `CodexSkillsAssembler` which mirrors `.claude/skills/` to `.agents/skills/`.

| # | Profile | Path |
|---|---------|------|
| 11 | go-gin | `tests/golden/go-gin/.agents/skills/x-test-plan/SKILL.md` |
| 12 | java-quarkus | `tests/golden/java-quarkus/.agents/skills/x-test-plan/SKILL.md` |
| 13 | java-spring | `tests/golden/java-spring/.agents/skills/x-test-plan/SKILL.md` |
| 14 | kotlin-ktor | `tests/golden/kotlin-ktor/.agents/skills/x-test-plan/SKILL.md` |
| 15 | python-click-cli | `tests/golden/python-click-cli/.agents/skills/x-test-plan/SKILL.md` |
| 16 | python-fastapi | `tests/golden/python-fastapi/.agents/skills/x-test-plan/SKILL.md` |
| 17 | rust-axum | `tests/golden/rust-axum/.agents/skills/x-test-plan/SKILL.md` |
| 18 | typescript-nestjs | `tests/golden/typescript-nestjs/.agents/skills/x-test-plan/SKILL.md` |

### 1.4 Golden Files -- `.github/skills/x-test-plan/SKILL.md` (8 profiles)

These are a condensed version produced by `GithubSkillsAssembler` from `resources/github-skills-templates/testing/x-test-plan.md`. Template substitution resolves `{language_name}` placeholders per profile.

| # | Profile | Path |
|---|---------|------|
| 19 | go-gin | `tests/golden/go-gin/.github/skills/x-test-plan/SKILL.md` |
| 20 | java-quarkus | `tests/golden/java-quarkus/.github/skills/x-test-plan/SKILL.md` |
| 21 | java-spring | `tests/golden/java-spring/.github/skills/x-test-plan/SKILL.md` |
| 22 | kotlin-ktor | `tests/golden/kotlin-ktor/.github/skills/x-test-plan/SKILL.md` |
| 23 | python-click-cli | `tests/golden/python-click-cli/.github/skills/x-test-plan/SKILL.md` |
| 24 | python-fastapi | `tests/golden/python-fastapi/.github/skills/x-test-plan/SKILL.md` |
| 25 | rust-axum | `tests/golden/rust-axum/.github/skills/x-test-plan/SKILL.md` |
| 26 | typescript-nestjs | `tests/golden/typescript-nestjs/.github/skills/x-test-plan/SKILL.md` |

### 1.5 Files NOT Modified

| File | Reason |
|------|--------|
| `src/assembler/skills-assembler.ts` | No change -- copies core skill templates as-is |
| `src/assembler/codex-skills-assembler.ts` | No change -- mirrors `.claude/skills/` to `.agents/skills/` |
| `src/assembler/github-skills-assembler.ts` | No change -- reads template, applies placeholder resolution |
| `src/domain/core-kp-routing.ts` | No change -- testing KP routing unaffected |
| `resources/core/03-testing-philosophy.md` | No change -- already contains TDD/TPP sections (story-0003-0001) |
| `resources/skills-templates/knowledge-packs/testing/SKILL.md` | No change -- KP entry point unaffected |
| `tests/node/integration/byte-for-byte.test.ts` | No change -- existing parity test validates golden files automatically |

### 1.6 Total File Count

- 2 template source files modified
- 24 golden files updated (8 profiles x 3 targets)
- **Total: 26 files**

---

## 2. Changes to x-test-plan SKILL.md (Claude/Agents Copy)

Source: `resources/skills-templates/core/x-test-plan/SKILL.md`

### 2.1 Sections Preserved (No Change)

| Section | Lines | Reason |
|---------|-------|--------|
| YAML frontmatter | 1-6 | Skill metadata unchanged |
| Global Output Policy | 8-12 | Output policy unchanged |
| `# Skill: Plan Tests (Orchestrator)` | 14 | Title unchanged |
| `## Purpose` | 16-18 | Purpose statement unchanged |
| `## Input` | 20-22 | Input specification unchanged |
| `## Execution Flow` | 24-32 | 3-step flow preserved but Step 2 description updated |
| `## Step 1: Gather Context (Subagent via Task)` | 34-63 | **FULLY PRESERVED** (RULE-009) -- subagent prompt, KP references, return schema |
| `## Anti-Patterns` | existing | Preserved, possibly extended |
| `## Integration Notes` | existing | Preserved |

### 2.2 Sections Modified

#### Step 2: Generate Test Scenarios (Lines 65-94 -- RESTRUCTURED)

**BEFORE:** Organized by category (Happy Path, Error Path, Boundary, Parametrized, Integration, API, E2E, Contract, Chaos).

**AFTER:** Organized by Double-Loop TDD structure with TPP ordering.

New structure:

```markdown
## Step 2: Generate Test Scenarios (Orchestrator -- Inline)

Using the context returned by the subagent, generate a Double-Loop TDD test plan.
Organize scenarios by implementation order (TPP), NOT by test category.

### 2.1 Acceptance Tests (Outer Loop)

For each Gherkin scenario in the story, generate an acceptance test entry:

| Field | Description |
|-------|-------------|
| ID | `AT-N` (sequential) |
| Gherkin | Reference to the original scenario |
| Status | RED until all unit tests for this acceptance criteria complete |
| Components | List of classes/modules under test |
| Test Type | Integration, API, or E2E (depending on story scope) |

### 2.2 Unit Tests (Inner Loop -- TPP Order)

Generate unit test scenarios in strict TPP order. Each scenario represents one
Red-Green-Refactor cycle.

#### TPP Level 1 -- Degenerate Cases
- Null, empty, zero inputs -> return default/error
- Transform: `{}->nil` or `nil->constant`

#### TPP Level 2 -- Unconditional Paths
- Single valid input -> direct output (no branching)
- Transform: `constant->variable`

#### TPP Level 3 -- Simple Conditions
- Single if/else branching
- Transform: `unconditional->conditional`

#### TPP Level 4 -- Complex Conditions
- Multiple branches, switch/match, compound boolean
- Transform: deeper conditional logic

#### TPP Level 5 -- Iterations
- Collection processing, loops, map/filter/reduce
- Transform: `scalar->collection`, `statement->recursion/iteration`

#### TPP Level 6 -- Edge Cases
- Boundary values (at-min, at-max, past-max)
- Transform: `value->mutated value`

For each unit test entry, include:

| Field | Required | Description |
|-------|----------|-------------|
| ID | M | `UT-N` (sequential within TPP order) |
| Test | M | What to test (method + scenario + expected) |
| Implementation | O | Minimum code to pass this test |
| Transform | M | TPP transformation applied |
| TPP Level | M | 1-6 |
| Components | M | Classes/modules needed |
| Depends on | M | Previous test IDs that are prerequisites, or `--` |
| Parallel | M | `yes` if independent of other tests at same level |

### 2.3 Integration Tests (Cross-Component)

Position AFTER the unit tests of the components involved.
Include only when multiple components interact (DB, HTTP, messaging).

| Field | Required | Description |
|-------|----------|-------------|
| ID | M | `IT-N` (sequential) |
| Test | M | What to test |
| Components | M | Interacting components |
| Depends on | M | UT IDs that must pass first |
| Parallel | M | `yes`/`no` |

### 2.4 CRUD-Only Story Optimization

When a story describes a purely CRUD operation without branching logic:
- UTs should cover: degenerate (Level 1) -> constant (Level 1) -> variable (Level 2)
- Do NOT generate conditional or iteration UTs unless the business rules demand them
- Acceptance tests should focus on the full CRUD flow
```

#### Step 3: Estimate & Validate (MODIFIED)

**BEFORE:** Coverage Estimation Table + Quality Checks.

**AFTER:** Coverage Estimation Table + Updated Quality Checks that include TPP ordering validation.

New quality checks to add:

```markdown
### Quality Checks

1. Every Gherkin scenario maps to >= 1 acceptance test (AT)
2. Every acceptance criterion maps to >= 1 unit test chain (UT)
3. UT-1 is ALWAYS a degenerate case (TPP Level 1)
4. UTs follow non-decreasing TPP level order
5. Every exception has >= 1 error path test
6. Boundary values use triplet pattern (at-min, at-max, past-max)
7. Dependency markers are complete (no orphan UTs)
8. Estimated coverage meets thresholds (>= 95% line, >= 90% branch)
9. Test naming follows convention: `[method]_[scenario]_[expected]`
10. No unnecessary UTs for CRUD-only stories (max Level 3 unless justified)
```

#### Output Format (RESTRUCTURED)

**BEFORE:** Organized by Test Class with Happy Path / Error Path / Boundary / Parametrized sub-sections.

**AFTER:** Organized by Double-Loop structure with TPP ordering.

New output template:

```markdown
# Test Plan -- STORY-ID: [Title]

## Summary
- Acceptance tests: X (from Y Gherkin scenarios)
- Unit tests: ~Z (in TPP order)
- Integration tests: ~W
- Estimated line coverage: ~P%
- Estimated branch coverage: ~Q%

## Acceptance Tests (Outer Loop)

### AT-1: [Gherkin scenario name]
- **Gherkin**: [reference to story scenario]
- **Status**: RED until all unit tests complete
- **Components**: [list of components under test]
- **Acceptance Criteria**: [what must be true for this AT to pass]

## Unit Tests (Inner Loop -- TPP Order)

### UT-1: [degenerate case description] -- TPP Level 1
- **Test**: [methodUnderTest]_[scenario]_[expectedBehavior]
- **Implementation**: [minimum code to pass]
- **Transform**: {}->nil
- **Components**: [class/module]
- **Depends on**: --
- **Parallel**: yes

### UT-2: [next case description] -- TPP Level 2
- **Test**: [methodUnderTest]_[scenario]_[expectedBehavior]
- **Implementation**: [minimum code to pass]
- **Transform**: constant->variable
- **Components**: [class/module]
- **Depends on**: UT-1
- **Parallel**: no

...

## Integration Tests (Cross-Component)

### IT-1: [integration scenario]
- **Test**: [description]
- **Components**: [interacting components]
- **Depends on**: UT-3, UT-7
- **Parallel**: no

## Coverage Estimation
[table]

## Risks and Gaps
- [Hard-to-test scenarios]
- [Coverage gaps needing attention]
```

### 2.3 Sections Added

| New Section | Location | Purpose |
|-------------|----------|---------|
| TPP Level sub-sections under Step 2.2 | Within Step 2 | Explicit ordering guide for each TPP level |
| Dependency markers spec | Within Step 2 field tables | Per-scenario metadata: depends-on, parallel, components |
| CRUD-only optimization | End of Step 2 | Prevent over-testing simple stories |
| Updated quality checks | Step 3 | TPP-aware validation rules |

### 2.4 Sections Removed

| Removed Section | Replacement |
|-----------------|-------------|
| `### 2.1 Happy Path` | Absorbed into TPP Level 2-3 unit tests |
| `### 2.2 Error Path` | Absorbed into TPP Level 1 (degenerate) and Level 3-4 (conditional) |
| `### 2.3 Boundary Tests` | Absorbed into TPP Level 6 (edge cases) |
| `### 2.4 Parametrized Tests` | Absorbed into relevant TPP levels (noted as parametrized where applicable) |
| `### 2.5 Integration Tests` | Moved to new `### 2.3 Integration Tests` section with dependency markers |
| `### 2.6 API Tests` | Absorbed into Acceptance Tests section (AT entries) |
| `### 2.7 E2E Tests` | Absorbed into Acceptance Tests section (AT entries) |
| `### 2.8 Contract Tests` | Retained as conditional note within integration tests |
| `### 2.9 Chaos Tests` | Retained as conditional note within integration tests |

---

## 3. Changes to x-test-plan.md (GitHub Copy)

Source: `resources/github-skills-templates/testing/x-test-plan.md`

The GitHub copy is a condensed version that mirrors the Claude copy's structure. Changes must maintain RULE-001 dual copy consistency.

### 3.1 Structural Changes

The GitHub copy currently has these sections:
- Purpose, Prerequisites, Knowledge Pack References
- Execution Flow (3-step)
- Step 1-3 (condensed)
- Test Naming Convention, Output, Anti-Patterns, Detailed References

**Modifications:**

| Section | Change |
|---------|--------|
| `### Step 2: Generate Test Scenarios` | Replace category table with Double-Loop + TPP structure (condensed) |
| Category table (Unit/Integration/API/E2E/Contract/Performance) | **REMOVE** -- replace with Double-Loop overview |
| `### Step 3: Estimate & Validate` | Add TPP-aware quality checks |
| `## Output` | Update output location description to mention Double-Loop format |
| `## Anti-Patterns` | Add: "Do NOT organize test plan by category -- use TPP order" |

### 3.2 New Content for GitHub Copy

```markdown
### Step 2: Generate Test Scenarios (Double-Loop + TPP Order)

Using the gathered context, generate a Double-Loop TDD test plan:

**Acceptance Tests (Outer Loop):**
- One AT per Gherkin scenario
- Status RED until all related unit tests pass
- Includes component list and acceptance criteria

**Unit Tests (Inner Loop -- TPP Order):**

| TPP Level | Scenarios | Transform |
|-----------|-----------|-----------|
| 1 -- Degenerate | Null, empty, zero | `{}->nil`, `nil->constant` |
| 2 -- Unconditional | Single path, no branching | `constant->variable` |
| 3 -- Simple conditions | Single if/else | `unconditional->conditional` |
| 4 -- Complex conditions | Multiple branches | Deeper conditionals |
| 5 -- Iterations | Collections, loops | `scalar->collection` |
| 6 -- Edge cases | Boundary values | `value->mutated value` |

Each UT includes: test name, implementation hint, TPP transform, dependencies, parallel flag.

**Integration Tests:** Positioned after unit tests of involved components.
```

### 3.3 Template Placeholder Note

The GitHub copy resolves `{language_name}` placeholders per profile (e.g., "typescript", "go"). The new content has no language-specific elements, so no new placeholders are needed.

---

## 4. Changes to Agents Copy

### 4.1 Generation Mechanism

The `.agents/skills/x-test-plan/SKILL.md` is **NOT** generated from a separate template. It is produced by `CodexSkillsAssembler` (`src/assembler/codex-skills-assembler.ts`), which performs a byte-for-byte copy of `.claude/skills/x-test-plan/SKILL.md`.

### 4.2 Impact

- **No separate template exists** for the agents copy.
- **No code changes needed** in `CodexSkillsAssembler`.
- The agents golden files will automatically match the `.claude/` golden files after update.
- When updating golden files, the `.agents/` copies must be updated to match the `.claude/` copies exactly.

### 4.3 Verification

After implementation, verify:
```bash
diff tests/golden/{profile}/.claude/skills/x-test-plan/SKILL.md \
     tests/golden/{profile}/.agents/skills/x-test-plan/SKILL.md
```
This must produce no output for all 8 profiles.

---

## 5. Golden File Update Strategy

### 5.1 Approach: Run the Generator

The most reliable method is to regenerate golden files by running the pipeline:

```bash
# For each profile, run the pipeline and copy output to golden
npx tsx src/cli.ts generate \
  --config resources/config-templates/setup-config.{profile}.yaml \
  --output /tmp/golden-{profile} \
  --resources resources

# Copy the x-test-plan skill from output to golden
cp /tmp/golden-{profile}/.claude/skills/x-test-plan/SKILL.md \
   tests/golden/{profile}/.claude/skills/x-test-plan/SKILL.md
cp /tmp/golden-{profile}/.agents/skills/x-test-plan/SKILL.md \
   tests/golden/{profile}/.agents/skills/x-test-plan/SKILL.md
cp /tmp/golden-{profile}/.github/skills/x-test-plan/SKILL.md \
   tests/golden/{profile}/.github/skills/x-test-plan/SKILL.md
```

### 5.2 Shortcut: Manual Copy

Since x-test-plan golden files are identical across all profiles for `.claude/` and `.agents/` (no template placeholders are resolved), we can:

1. Update the source template `resources/skills-templates/core/x-test-plan/SKILL.md`
2. Copy it to all 8 `.claude/` golden paths
3. Copy it to all 8 `.agents/` golden paths (identical content)
4. Update the GitHub source template `resources/github-skills-templates/testing/x-test-plan.md`
5. For `.github/` golden files, run the pipeline or manually apply `{language_name}` substitution per profile

### 5.3 Verification via Existing Tests

The `byte-for-byte.test.ts` integration test (`tests/node/integration/byte-for-byte.test.ts`) will automatically validate that pipeline output matches golden files. Run:

```bash
npx vitest run tests/node/integration/byte-for-byte.test.ts
```

### 5.4 Profile-Specific Differences

| Target | Profile-specific? | Placeholder |
|--------|-------------------|-------------|
| `.claude/` | No -- all 8 profiles identical | `{{LANGUAGE}}` stays unresolved |
| `.agents/` | No -- mirrors `.claude/` exactly | `{{LANGUAGE}}` stays unresolved |
| `.github/` | Yes -- `{language_name}` resolved per profile | `{language_name}` -> "typescript", "go", "java", etc. |

---

## 6. Backward Compatibility Assessment (RULE-003)

### 6.1 Breaking Changes

The output format of x-test-plan is **fundamentally restructured**:
- **BEFORE:** Organized by test category (Happy Path, Error Path, Boundary, etc.)
- **AFTER:** Organized by Double-Loop structure (AT, UT in TPP order, IT)

This is a **non-backward-compatible** change to the test plan output format.

### 6.2 Impact Analysis

| Consumer | Impact | Mitigation |
|----------|--------|------------|
| `x-dev-lifecycle` (Phase 1B) | Reads test plan output | Low risk -- lifecycle consumes plan as free-form markdown; no parser |
| `x-lib-task-decomposer` | Maps test scenarios to tasks | **Medium risk** -- must verify it can parse UT-N format for task mapping |
| Human developers | Read test plan for implementation guidance | Positive -- TPP ordering is more actionable |
| QA engineer agent (Phase 3) | Validates coverage in test plan | Low risk -- coverage estimation table format unchanged |

### 6.3 Mitigation

- The test plan output is consumed as markdown text by downstream skills, not parsed programmatically.
- Story 0003-0007 Gherkin scenario 6 explicitly requires compatibility with `x-lib-task-decomposer`. Verify by reading the task decomposer skill to confirm it does not depend on the old section headers (Happy Path, Error Path, etc.).
- The Coverage Estimation table and Summary sections remain structurally similar.

### 6.4 Recommendation

**Full replacement** (no dual format support). The old category-based format provided no implementation ordering; maintaining it would defeat the story's purpose. The change is additive in value and the output is consumed as text, not parsed.

---

## 7. Subagent Pattern Preservation (RULE-009)

### 7.1 Current Subagent Pattern

Step 1 of x-test-plan launches a **single** `general-purpose` subagent with a detailed prompt that:
1. Reads 3 knowledge packs (testing-philosophy, testing-conventions, architecture-principles)
2. Reads the story file
3. Scans existing code
4. Returns an 11-point structured context summary

### 7.2 Changes to Subagent

**NONE.** The subagent prompt in Step 1 is preserved exactly as-is. The subagent's 11-point return schema remains unchanged:

1. Test categories applicable
2. Naming convention
3. Test frameworks and assertion libraries
4. Fixture pattern
5. Acceptance criteria extracted from story
6. Business rules extracted
7. Exception types in scope
8. Layer boundaries
9. Existing patterns
10. Contract tests applicable
11. Chaos tests applicable

### 7.3 Why Subagent Needs No Change

The subagent gathers **raw context** from KPs and the story. The **orchestrator** (Step 2) is responsible for organizing that context into the test plan format. Since only Step 2's organization changes (from category-based to TPP-based), the subagent's context-gathering role is unaffected.

### 7.4 Verification

After implementation, confirm:
- Step 1 prompt text is character-for-character identical to current version
- The subagent `allowed-tools` remain `Read, Grep, Glob`
- The subagent return schema has exactly 11 items

---

## 8. Risk Assessment

### 8.1 High Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| `x-lib-task-decomposer` incompatibility | Medium | High | Read task decomposer skill before implementation; verify no hard dependency on old section headers |

### 8.2 Medium Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Golden file mismatch for `.github/` profiles | Medium | Medium | Run full pipeline for all 8 profiles; do not manually edit `.github/` goldens |
| Overly prescriptive TPP levels for simple stories | Medium | Medium | Include CRUD-only optimization section (2.4) |
| Increased skill template size | Low | Medium | Keep instructions concise; use tables over prose |

### 8.3 Low Risk

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Subagent prompt accidentally modified | Low | High | Use exact string matching in edit operations; verify with diff |
| `{{LANGUAGE}}` placeholders broken | Low | Medium | These are intentionally unresolved in `.claude/` copy; verify they remain |
| byte-for-byte test failures | Expected | Low | Update all 24 golden files before running tests |

---

## 9. Implementation Order (Recommended)

### Phase 1: Research (Pre-Implementation)
1. Read `x-lib-task-decomposer` to verify no hard parsing of old format
2. Confirm story-0003-0001 is complete (TDD/TPP sections in testing-philosophy.md)

### Phase 2: Template Changes (TDD -- Test First)
3. Write golden file tests (update expected content) -- RED
4. Modify `resources/skills-templates/core/x-test-plan/SKILL.md` -- GREEN
5. Update all 8 `.claude/` golden files (copy from template)
6. Update all 8 `.agents/` golden files (copy from `.claude/`)
7. Run byte-for-byte tests -- should pass for `.claude/` and `.agents/`

### Phase 3: GitHub Copy (TDD -- Test First)
8. Modify `resources/github-skills-templates/testing/x-test-plan.md`
9. Regenerate `.github/` golden files via pipeline (or manual substitution)
10. Run byte-for-byte tests -- should pass for all 3 targets

### Phase 4: Verification
11. Run full test suite: `npm test`
12. Verify coverage >= 95% line, >= 90% branch
13. Verify no compilation errors: `npx tsc --noEmit`

---

## 10. Summary of Changes

| Metric | Value |
|--------|-------|
| Template files modified | 2 |
| Golden files updated | 24 (8 profiles x 3 targets) |
| Source code (TypeScript) modified | 0 |
| New files created | 0 |
| Files deleted | 0 |
| Subagent changes | None (RULE-009 preserved) |
| Backward compatible | No (intentional -- output format restructured) |
| Test infrastructure changes | None (existing byte-for-byte tests validate) |
