# QA Review — story-0004-0011

```
ENGINEER: QA
STORY: story-0004-0011
SCORE: 35/36
STATUS: Rejected
---
PASSED:
- [QA-01] Test exists for each acceptance criterion (6 Gherkin scenarios in story) (2/2)
  Scenario 1 (CI workflow always): assemble_minimalConfig_generatesCiWorkflow + assemble_typescriptNpm_ciContainsNodeSetup
  Scenario 2 (Dockerfile when docker): assemble_containerDocker_generatesDockerfile + assemble_dockerfile_containsMultiStageBuild + assemble_dockerfile_containsHealthCheck
  Scenario 3 (K8s when kubernetes): assemble_orchestratorKubernetes_generatesDeployment + _generatesConfigMap
  Scenario 4 (Docker skipped when none): assemble_containerNone_skipsDockerfile + _skipsDockerCompose
  Scenario 5 (Deploy runbook): assemble_runbook_containsProcedureSection + _containsVerificationSection + _containsRollbackSection
  Scenario 6 (Smoke tests when true): assemble_smokeTestsTrue_generatesSmokeConfig + assemble_smokeTestsFalse_skipsSmokeConfig

- [QA-02] Line coverage >= 95% for new code (2/2)
  cicd-assembler.ts: 100% statements, 100% lines. Overall: 99.5% lines.

- [QA-03] Branch coverage >= 90% for new code (2/2)
  cicd-assembler.ts: 100% branch coverage. Overall: 97.67% branch.

- [QA-04] Test naming convention: methodUnderTest_scenario_expectedBehavior (2/2)
  All 61 tests follow the convention: assemble_{scenario}_{expectedBehavior}
  Examples: assemble_minimalConfig_generatesCiWorkflow, assemble_containerDocker_generatesDockerfile

- [QA-05] AAA pattern (Arrange-Act-Assert) in each test (2/2)
  All tests follow Arrange (buildConfig + TemplateEngine), Act (assembler.assemble), Assert (expect).
  Clean separation visible in every test body.

- [QA-06] Parametrized tests for data-driven scenarios (per-stack tests) (2/2)
  STACKS array with 7 entries (java-maven, java-gradle, kotlin-gradle, typescript-npm, python-pip, go-go, rust-cargo).
  Two it.each blocks: one for Dockerfile correctness, one for CI workflow correctness.
  Total: 14 parametrized test executions.

- [QA-07] Exception/error paths tested (missing templates, unknown language) (2/2)
  assemble_ciTemplatesMissing_returnsWithWarning: empty resources dir triggers warnings.
  assemble_unknownFramework_usesDefaultPort: unknown-fw falls back to 8080.
  assemble_unknownFramework_usesDefaultHealthPath: unknown-fw falls back to /health.

- [QA-08] No test interdependency (each test independent) (2/2)
  Each test creates its own config, engine, and runs assemble independently.
  beforeEach creates fresh tmpDir/outputDir; afterEach cleans up.
  No shared mutable state between tests.

- [QA-09] Fixtures centralized (helper functions for config building) (2/2)
  buildConfig() factory function with override pattern at line 23-59.
  Follows the project convention (Partial overrides on a default config).

- [QA-10] Unique test data (no shared mutable state) (2/2)
  Each test uses fs.mkdtempSync for unique temp directories.
  No static mutable variables. assembler instance created fresh in beforeEach.

- [QA-11] Edge cases covered (container=none, orchestrator=none, smokeTests=false) (2/2)
  "minimal config -- all disabled" describe block: container=none, orchestrator=none, smokeTests=false.
  Verifies only 2 files generated (CI + runbook) and >= 3 warnings.
  "partial config -- Docker without K8s" verifies Docker present but K8s absent.

- [QA-12] Integration tests for golden file parity (byte-for-byte) (2/2)
  40 byte-for-byte integration tests pass across all 8 profiles.
  Golden files updated in commit 4336565 for all profiles with new CI/CD artifacts.
  Dockerfile, docker-compose.yml, k8s/, smoke-config.md, deploy-runbook.md all verified.

- [QA-13] Commits show test-first pattern (test commit before implementation) (2/2)
  eebd777 test(cicd): add CicdAssembler unit tests [TDD:RED] -- test file only
  f51e933 feat(cicd): implement CicdAssembler [TDD:GREEN] -- production code + minor test adjustments
  7decff7 refactor(cicd): extract GenerationOutput [TDD:REFACTOR] -- production code only
  Clear RED -> GREEN -> REFACTOR sequence.

- [QA-14] Explicit refactoring after green (REFACTOR commit exists) (2/2)
  Commit 7decff7 is tagged [TDD:REFACTOR]. Extracts GenerationOutput interface, reduces
  parameter counts from 5-7 to 4-6. Production-only change, all 61 tests remain green.

- [QA-15] Tests follow TPP progression (degenerate -> simple -> compound -> edge) (2/2)
  Test order: always-generated (degenerate) -> conditional Docker (simple condition) ->
  conditional K8s (simple condition) -> conditional smoke tests (simple condition) ->
  full config (compound) -> partial config (compound) -> minimal config (compound) ->
  template substitution (variable) -> content validation (variable) -> edge cases (edge).

- [QA-17] Acceptance tests validate E2E behavior (integration tests pass) (2/2)
  40 byte-for-byte integration tests pass. Pipeline test updated to 18 assemblers.
  codex-config-assembler test updated to 18 assemblers. All 1791 tests pass.

- [QA-18] TDD coverage thresholds maintained (overall >= 95% line, >= 90% branch) (2/2)
  Overall: 99.5% lines, 97.67% branch. Both exceed thresholds.
  New code (cicd-assembler.ts): 100% statements, 100% branch, 100% functions, 100% lines.

PARTIAL:
- [QA-16] No test written after implementation (all tests in RED phase) (1/2)
  — tests/node/assembler/cicd-assembler.test.ts:510,868 — Improvement: The GREEN commit (f51e933) modified 3 test assertions (renamed test, changed placeholder checks from generic `{{ ` to specific `{{ project_name }}`). While these are minor refinements to make tests more precise rather than new behavior tests, strict TDD discipline requires tests to be frozen after the RED phase. The test intent did not change and no new tests were added, so this is a minor deviation. [LOW]
```

## Evidence Summary

| Metric | Value |
|--------|-------|
| Test file | tests/node/assembler/cicd-assembler.test.ts (876 lines, 61 tests) |
| Production file | src/assembler/cicd-assembler.ts (243 lines) |
| New code line coverage | 100% |
| New code branch coverage | 100% |
| Overall line coverage | 99.5% |
| Overall branch coverage | 97.67% |
| Total tests in suite | 1,791 (all passing) |
| Integration tests | 40 byte-for-byte (all passing) |
| Commit count | 7 (templates, RED, GREEN, REFACTOR, pipeline, golden, fix) |
| TDD markers | [TDD:RED], [TDD:GREEN], [TDD:REFACTOR] present |
| Gherkin scenarios covered | 6/6 |

## Commit Sequence

| Order | Hash | Tag | Description |
|-------|------|-----|-------------|
| 1 | 520a6e1 | — | CI/CD artifact templates (resources) |
| 2 | eebd777 | [TDD:RED] | 49 unit tests, CicdAssembler does not exist |
| 3 | f51e933 | [TDD:GREEN] | CicdAssembler implementation, 61 tests pass |
| 4 | 7decff7 | [TDD:REFACTOR] | Extract GenerationOutput, reduce params |
| 5 | ad76cfd | — | Register CicdAssembler in pipeline |
| 6 | 4336565 | — | Update golden files for all 8 profiles |
| 7 | 9c29af4 | — | Fix codex-config-assembler test count |

## Recommendation

Score 35/36. Single partial item (QA-16) is LOW severity -- the GREEN commit adjusted 3 assertion strings for precision but added no new test behavior. This is a borderline case: the test *intent* was unchanged. For strict TDD purists, tests should be completely frozen between RED and GREEN phases. However, the adjustments were refinements to assertion specificity (from `{{ ` to `{{ project_name }}`), not new test cases.

**Verdict: Rejected (1 item below 2/2).** Fix: move the assertion refinements to the REFACTOR commit or a separate `test(cicd): refine assertion specificity` commit to maintain strict RED phase immutability.
