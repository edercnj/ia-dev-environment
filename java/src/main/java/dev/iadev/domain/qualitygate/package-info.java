/**
 * Quality Gate scoring engine for story specifications.
 *
 * <p>Evaluates the quality of Gherkin scenarios, data contracts,
 * and dependency references in story markdown files. Produces a
 * normalized score (0-100) and actionable feedback.</p>
 *
 * <p>This package has zero framework dependencies.
 * It depends only on the Java standard library.</p>
 */
package dev.iadev.domain.qualitygate;
