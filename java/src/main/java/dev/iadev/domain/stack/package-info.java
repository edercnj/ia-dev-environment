/**
 * Stack resolution, validation, and domain mappings.
 *
 * <p>Contains pure domain logic for resolving build commands,
 * validating language/framework combinations, and mapping stacks
 * to knowledge packs, patterns, and protocols.
 *
 * <p>This package must NOT import any external framework classes
 * (Picocli, Pebble, Jackson, SnakeYAML, JLine).
 *
 * @see dev.iadev.domain.model.ProjectConfig
 */
package dev.iadev.domain.stack;
