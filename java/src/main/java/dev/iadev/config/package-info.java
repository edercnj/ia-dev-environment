/**
 * Configuration loading and validation from YAML files.
 *
 * <p>Handles parsing of YAML configuration into domain model objects
 * and validating required fields and cross-field constraints.
 */
package dev.iadev.config;
