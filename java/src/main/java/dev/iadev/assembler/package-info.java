/**
 * Assemblers that compose the artifact generation pipeline.
 *
 * <p>Each assembler implements a uniform interface to generate a specific
 * category of output files (rules, skills, agents, etc.). Assemblers are
 * executed in a fixed order defined by RULE-005.
 */
package dev.iadev.assembler;
