/**
 * CI-grade linters and validators for the ia-dev-env project.
 *
 * <p>Tools in this package enforce invariants that cannot be captured by
 * the generator tests (e.g., telemetry marker balance in
 * {@code SKILL.md}). They are intended to run both inside acceptance
 * tests and as part of the CI pipeline.</p>
 */
package dev.iadev.ci;
