/**
 * Utility classes for I/O, path handling, and security.
 *
 * <p>Includes path safety validation (RULE-011), atomic file operations
 * (RULE-008), and cross-platform path handling (RULE-009).
 */
package dev.iadev.util;
