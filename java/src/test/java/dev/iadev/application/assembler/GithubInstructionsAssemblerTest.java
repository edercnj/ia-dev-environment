package dev.iadev.application.assembler;

/**
 * Tests for GithubInstructionsAssembler have been split
 * into focused test classes:
 * <ul>
 *   <li>{@link GithubInstructionsCopilotTest}
 *       — interface contract + copilot instructions</li>
 *   <li>{@link GithubInstructionsFormatTest}
 *       — formatInterfaces, formatFrameworkVersion,
 *       placeholders</li>
 *   <li>{@link GithubInstructionsFileGenTest}
 *       — file generation + edge cases</li>
 *   <li>{@link GithubInstructionsGoldenTest}
 *       — golden file parity</li>
 * </ul>
 *
 * <p>Shared fixtures:
 * {@link GithubInstructionsTestFixtures}.</p>
 *
 * @see GithubInstructionsCopilotTest
 * @see GithubInstructionsFormatTest
 * @see GithubInstructionsFileGenTest
 * @see GithubInstructionsGoldenTest
 */
// This file intentionally left as a documentation
// pointer. All 29 test methods have been migrated
// to the classes listed above.
class GithubInstructionsAssemblerTest {
}
