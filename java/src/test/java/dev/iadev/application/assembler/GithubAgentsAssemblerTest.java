package dev.iadev.assembler;

/**
 * Tests for GithubAgentsAssembler have been split:
 * <ul>
 *   <li>{@link GithubAgentsConditionalTest}
 *       — interface + conditional selection</li>
 *   <li>{@link GithubAgentsEventTest}
 *       — event conditions + empty result</li>
 *   <li>{@link GithubAgentsRenderCoreTest}
 *       — render, core, developer, warnings,
 *       pipeline, result contract</li>
 * </ul>
 */
class GithubAgentsAssemblerTest {
}
