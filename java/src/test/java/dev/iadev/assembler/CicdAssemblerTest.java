package dev.iadev.assembler;

/**
 * Tests for CicdAssembler have been split into:
 * <ul>
 *   <li>{@link CicdStackContextTest}
 *       — interface, buildStackContext,
 *       LINT_COMMANDS</li>
 *   <li>{@link CicdAssembleConditionalTest}
 *       — assemble conditional generation</li>
 * </ul>
 */
class CicdAssemblerTest {
}
