package dev.iadev.domain.stack;

/**
 * Tests for StackValidator have been split into:
 * <ul>
 *   <li>{@link StackValidatorLangFrameworkTest}
 *       — validateStack, validateLanguageFramework</li>
 *   <li>{@link StackValidatorVersionTest}
 *       — Java version, Django version, native build,
 *       interface types, architecture style,
 *       extractMajor, extractMinor</li>
 * </ul>
 */
class StackValidatorTest {
}
