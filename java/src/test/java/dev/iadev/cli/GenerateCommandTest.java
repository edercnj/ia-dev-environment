package dev.iadev.cli;

/**
 * Tests for GenerateCommand have been split into:
 * <ul>
 *   <li>{@link GenerateCommandHelpTest}
 *       — help, options, mutual exclusivity,
 *       no input, config not found</li>
 *   <li>{@link GenerateCommandExecTest}
 *       — stack, dry-run, force, overwrite, dangerous,
 *       verbose, valid/invalid config, performance</li>
 * </ul>
 */
class GenerateCommandTest {
}
