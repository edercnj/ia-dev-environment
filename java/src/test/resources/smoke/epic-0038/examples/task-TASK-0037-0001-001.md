# Task: Criar arquivo de regra `14-worktree-lifecycle.md`

**ID:** TASK-0037-0001-001
**Story:** story-0037-0001
**Status:** Concluída

> **Exemplo migrado** — este arquivo é uma reescrita retroativa da sub-seção original de
> `plans/epic-0037/story-0037-0001.md` §7 para o novo schema canônico de
> `task-TASK-XXXX-YYYY-NNN.md` introduzido por EPIC-0038 (story-0038-0001). Serve como
> fixture de teste para `TaskFileParser` / `TaskValidator` e como referência operacional
> para futuros autores de tasks. O comportamento real da task já foi implementado e
> mergeado em develop; nenhum re-trabalho de execução é implicado por esta migração.

## 1. Objetivo

Criar o arquivo normativo `java/src/main/resources/targets/claude/rules/14-worktree-lifecycle.md` contendo as 7 seções da RULE-14 (naming, creator-owns-removal matrix, anti-patterns), consumindo-se a spec em `plans/epic-0037/spec-rule-14-worktree-lifecycle.md` e produzindo uma mudança commitável atômica (um arquivo, um commit).

## 2. Contratos I/O

### 2.1 Inputs

- Branch `feature/task-0037-0001-001-rule-file` criada a partir de `feature/story-0037-0001-rule-14-worktree-lifecycle`
- Spec `plans/epic-0037/spec-rule-14-worktree-lifecycle.md` §3-§7 disponível como fonte normativa
- Diretório `java/src/main/resources/targets/claude/rules/` existente com rules 01-13 já presentes
- Naming convention do épico-0037 disponível (rule files = `NN-name.md`, sem front-matter)

### 2.2 Outputs

- Arquivo `java/src/main/resources/targets/claude/rules/14-worktree-lifecycle.md` existe (verificável via `ls`)
- 7 seções presentes: Naming, Canonical Naming Examples, Creator-Owns-Removal Matrix, Protected Paths, Anti-Patterns, Cleanup Procedures, Acceptance Criteria (verificável: `grep -c "^## " > 6`)
- Tabela "Naming Convention" com ≥ 4 linhas (verificável: `awk '/## Naming/,/## Canonical/' | grep -c "^|"`)
- Matriz "Creator-Owns-Removal" com ≥ 5 linhas (verificável: `awk '/Creator-Owns-Removal/,/## Protected/' | grep -c "^|"`)
- Seção "Anti-Patterns" lista ≥ 3 anti-padrões explícitos com remediação (verificável: `grep -c "^- \*\*"` dentro da seção)
- Build do Maven continua verde (`mvn compile` sem erros — rule files não afetam bytecode, mas a infraestrutura de copy-resources não quebra)

### 2.3 Testabilidade

- [x] Independentemente testável
- [ ] Requer mock de TASK-XXXX-YYYY-NNN
- [ ] Coalescível com TASK-XXXX-YYYY-NNN

> **Comentário sobre os três kinds** (AC de TASK-0038-0001-005):
>
> - **INDEPENDENT (marcado):** a task cria um arquivo markdown isolado. Verificação é por grep e contagem de seções — input puro (a spec) produz output puro (um arquivo). Nenhuma outra task precisa estar pronta.
> - **REQUIRES_MOCK (não aplicável aqui):** se esta task dependesse da API de `RuleFileWriter` (classe produzida por outra task ainda não implementada), marcaríamos `[x] Requer mock de TASK-YYY` e os testes injetariam um stub implementando a interface de `RuleFileWriter`.
> - **COALESCED (não aplicável aqui):** se esta task e TASK-0037-0001-002 (substituir bloco inline em `x-git-worktree/SKILL.md`) tivessem recursão mútua de formatação de referência cruzada, marcaríamos `[x] Coalescível com TASK-0037-0001-002` e commitaríamos ambas no mesmo commit atômico. Não é o caso — 002 depende de 001 mas 001 não depende de 002 (DAG acíclico).

## 3. Definition of Done

- [x] Arquivo `14-worktree-lifecycle.md` criado e commitado
- [x] 7 seções presentes conforme §2.2 Outputs
- [x] Tabela de naming convention com ≥ 4 linhas
- [x] Matriz creator-owns-removal com ≥ 5 linhas
- [x] ≥ 3 anti-patterns com remediação explícita
- [x] `mvn compile` verde
- [x] Red → Green → Refactor honesto: N/A para doc-only (registrado no PR original — verificação via grep, sem teste unit)
- [x] Commit atômico em Conventional Commits: `docs(rule): add 14-worktree-lifecycle.md`

## 4. Dependências

| Depends on | Relação | Pode mockar? |
| :--- | :--- | :--- |
| — | — | — |

## 5. Plano de implementação

Ver `plan-task-TASK-0037-0001-001.md` (gerado retroativamente por `x-task-plan` quando story-0038-0003 for concluída).
