apiVersion: v1
kind: Service
metadata:
  name: my-spring-service
  labels:
    app: my-spring-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: my-spring-service
