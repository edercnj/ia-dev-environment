# Lifecycle Integrity Audit Baseline (EPIC-0046 story-0046-0007)
#
# Format: DIMENSION|relative/path/to/SKILL.md[|LINE]
#
# Entries listed here are tolerated by LifecycleIntegrityAuditTest while
# the Core Loop / documentation structure is incrementally tightened.
# Any violation NOT in this list fails the build with
# LIFECYCLE_AUDIT_REGRESSION.
#
# Entries without an explicit line number match any line in that file
# for that dimension (tolerant to re-numbering on edits).
#
# Remove an entry only when you have verified the corresponding sub-
# section is actually wired into the skill's Core Loop — then the
# orphan disappears organically from the scan.
#
# ORPHAN_PHASE baseline — SKILL.md files whose numbered sub-sections
# (X.Y style headings) are legitimate TOC/documentation structure and
# are not expected to be cross-referenced from a Core Loop block.
ORPHAN_PHASE|targets/claude/skills/core/dev/x-story-implement/SKILL.md
ORPHAN_PHASE|targets/claude/skills/core/lib/x-lib-task-decomposer/SKILL.md
ORPHAN_PHASE|targets/claude/skills/core/ops/x-release/SKILL.md
ORPHAN_PHASE|targets/claude/skills/core/pr/x-pr-merge-train/SKILL.md
ORPHAN_PHASE|targets/claude/skills/core/review/x-review-qa/SKILL.md
