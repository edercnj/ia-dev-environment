apiVersion: v1
kind: Service
metadata:
  name: my-fastapi-service
  labels:
    app: my-fastapi-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: my-fastapi-service
