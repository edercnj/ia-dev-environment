apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-fastapi-service
  labels:
    app: my-fastapi-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-fastapi-service
  template:
    metadata:
      labels:
        app: my-fastapi-service
    spec:
      containers:
        - name: my-fastapi-service
          image: my-fastapi-service:latest
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          envFrom:
            - configMapRef:
                name: my-fastapi-service-config
