apiVersion: v1
kind: Service
metadata:
  name: my-quarkus-service
  labels:
    app: my-quarkus-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: my-quarkus-service
