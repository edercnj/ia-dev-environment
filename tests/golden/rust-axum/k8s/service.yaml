apiVersion: v1
kind: Service
metadata:
  name: my-rust-service
  labels:
    app: my-rust-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: my-rust-service
