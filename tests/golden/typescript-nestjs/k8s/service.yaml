apiVersion: v1
kind: Service
metadata:
  name: my-nestjs-service
  labels:
    app: my-nestjs-service
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: my-nestjs-service
