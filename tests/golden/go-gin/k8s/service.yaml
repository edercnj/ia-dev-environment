apiVersion: v1
kind: Service
metadata:
  name: my-go-service
  labels:
    app: my-go-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: my-go-service
