apiVersion: v1
kind: Service
metadata:
  name: my-ktor-service
  labels:
    app: my-ktor-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: my-ktor-service
