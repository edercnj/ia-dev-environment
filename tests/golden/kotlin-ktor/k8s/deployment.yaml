apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-ktor-service
  labels:
    app: my-ktor-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-ktor-service
  template:
    metadata:
      labels:
        app: my-ktor-service
    spec:
      containers:
        - name: my-ktor-service
          image: my-ktor-service:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          envFrom:
            - configMapRef:
                name: my-ktor-service-config
