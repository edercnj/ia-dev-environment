# Implementation Plan — story-0003-0004

## Rule 13: Enriched Gherkin in Story Decomposition

### 1. Summary

This story modifies a single source-of-truth Markdown file (`resources/core/13-story-decomposition.md`) to enrich three areas of Rule 13: SD-02 gains Gherkin completeness requirements, SD-05 raises the minimum scenario count from 2 to 4, and a new scenario ordering guideline is added. Because the pipeline performs a byte-for-byte copy from source to output, all downstream copies (generated output, golden files) must be updated to match.

---

### 2. Architecture — How the Dual Copy System Works

```
resources/core/13-story-decomposition.md          <-- SOURCE OF TRUTH (edit here)
        |
        | fs.copyFileSync (no transformation)
        | via: src/assembler/rules-assembler.ts :: routeCoreToKps()
        | routing: src/domain/core-kp-routing.ts (13-story-decomposition.md -> story-planning/references/story-decomposition.md)
        v
{outputDir}/skills/story-planning/references/story-decomposition.md   <-- GENERATED OUTPUT
        |
        | byte-for-byte comparison
        v
tests/golden/{profile}/.claude/skills/story-planning/references/story-decomposition.md   <-- GOLDEN FILES (8 profiles x 2 dirs)
tests/golden/{profile}/.agents/skills/story-planning/references/story-decomposition.md
```

Key properties:
- The pipeline (`routeCoreToKps`) uses `fs.copyFileSync` -- zero transformation. The output is identical to the source.
- The routing entry in `core-kp-routing.ts` maps `13-story-decomposition.md` -> KP `story-planning` -> dest `story-decomposition.md`. **This does not change** because the filename stays the same.
- Golden files are compared byte-for-byte in `tests/node/integration/byte-for-byte.test.ts`.
- All 8 profiles produce identical copies (the file is profile-independent).

---

### 3. Affected Layers and Components

| Layer | Component | Impact |
|-------|-----------|--------|
| Resources (source of truth) | `resources/core/13-story-decomposition.md` | **MODIFIED** -- primary edit target |
| Domain (routing) | `src/domain/core-kp-routing.ts` | **UNCHANGED** -- filename does not change |
| Assembler (pipeline) | `src/assembler/rules-assembler.ts` | **UNCHANGED** -- copy logic unchanged |
| Generated copies (workspace) | `skills/`, `.claude/`, `.agents/` | **STALE** -- already out of sync; not committed to git as generated output. Will be regenerated by pipeline. |
| Golden files (test fixtures) | `tests/golden/{profile}/.claude/...` and `.agents/...` | **MUST UPDATE** -- 16 files total |
| Tests | `tests/node/integration/byte-for-byte.test.ts` | **UNCHANGED** -- test logic unchanged, only golden file content changes |
| Skills metadata | `skills/story-planning/SKILL.md` | **UNCHANGED** -- description references remain valid |

---

### 4. Files to Modify (Exact Paths)

#### 4.1 Source of Truth (1 file)

| # | File | Change |
|---|------|--------|
| 1 | `resources/core/13-story-decomposition.md` | All three story requirements (see Section 5) |

#### 4.2 Golden Files (16 files -- 8 profiles x 2 directories)

All golden files must be identical to the updated source of truth.

**.claude/ directory (8 files):**

| # | File |
|---|------|
| 2 | `tests/golden/go-gin/.claude/skills/story-planning/references/story-decomposition.md` |
| 3 | `tests/golden/java-quarkus/.claude/skills/story-planning/references/story-decomposition.md` |
| 4 | `tests/golden/java-spring/.claude/skills/story-planning/references/story-decomposition.md` |
| 5 | `tests/golden/kotlin-ktor/.claude/skills/story-planning/references/story-decomposition.md` |
| 6 | `tests/golden/python-click-cli/.claude/skills/story-planning/references/story-decomposition.md` |
| 7 | `tests/golden/python-fastapi/.claude/skills/story-planning/references/story-decomposition.md` |
| 8 | `tests/golden/rust-axum/.claude/skills/story-planning/references/story-decomposition.md` |
| 9 | `tests/golden/typescript-nestjs/.claude/skills/story-planning/references/story-decomposition.md` |

**.agents/ directory (8 files):**

| # | File |
|---|------|
| 10 | `tests/golden/go-gin/.agents/skills/story-planning/references/story-decomposition.md` |
| 11 | `tests/golden/java-quarkus/.agents/skills/story-planning/references/story-decomposition.md` |
| 12 | `tests/golden/java-spring/.agents/skills/story-planning/references/story-decomposition.md` |
| 13 | `tests/golden/kotlin-ktor/.agents/skills/story-planning/references/story-decomposition.md` |
| 14 | `tests/golden/python-click-cli/.agents/skills/story-planning/references/story-decomposition.md` |
| 15 | `tests/golden/python-fastapi/.agents/skills/story-planning/references/story-decomposition.md` |
| 16 | `tests/golden/rust-axum/.agents/skills/story-planning/references/story-decomposition.md` |
| 17 | `tests/golden/typescript-nestjs/.agents/skills/story-planning/references/story-decomposition.md` |

**Total: 17 files modified** (1 source + 16 golden copies)

#### 4.3 Files Explicitly UNCHANGED

| File | Reason |
|------|--------|
| `src/domain/core-kp-routing.ts` | Filename `13-story-decomposition.md` unchanged; routing entry stays the same |
| `src/assembler/rules-assembler.ts` | Copy logic unchanged; still `fs.copyFileSync` |
| `tests/node/integration/byte-for-byte.test.ts` | Test logic unchanged; it compares pipeline output vs golden files |
| `tests/helpers/integration-constants.ts` | No new profiles or constants needed |
| `skills/story-planning/SKILL.md` | Skill description still accurate; "story-decomposition.md" reference unchanged |
| `skills/story-planning/references/story-decomposition.md` | Generated copy -- pipeline will regenerate. Not committed. |
| `.claude/skills/story-planning/references/story-decomposition.md` | Generated copy -- pipeline will regenerate. Not committed. |
| `.agents/skills/story-planning/references/story-decomposition.md` | Generated copy -- pipeline will regenerate. Not committed. |

---

### 5. Detailed Changes to Source of Truth

All edits target `resources/core/13-story-decomposition.md`.

#### 5.1 SD-02 Enhancement -- Gherkin Completeness

**Location:** After the existing SD-02 bullet list (after line 32: `- **Sub-tasks** individually estimable...`) and before the code example block.

**Add a new sub-section within SD-02:**

```markdown
### Gherkin Completeness Requirements

Every story's Gherkin acceptance criteria MUST include:

- **Degenerate cases** (mandatory): At minimum, scenarios for null input, empty collection, zero value, and missing required field
- **Boundary values** (mandatory): Triplet pattern for each bounded input — (at-minimum, at-maximum, past-maximum)
- **Error paths** (complete): Every documented error type in the specification MUST have a corresponding Gherkin scenario with expected error code/message
- **Minimum floor**: 4 scenarios per story (1 happy path + 2 error paths + 1 edge case is the floor, not the ceiling)
```

Also update the existing SD-02 code example to reflect the new minimum (change `6 Gherkin scenarios` to `6 Gherkin scenarios (minimum 4: happy + errors + edge cases)`).

#### 5.2 SD-05 Enhancement -- Sizing with Gherkin Counts

**Location:** SD-05 MINIMUM table (line 100-103).

**Change the Gherkin scenarios minimum row:**

| Before | After |
|--------|-------|
| `Gherkin scenarios \| 2 \| Merge -- too small to be a story` | `Gherkin scenarios \| 4 \| Add degenerate/boundary/error scenarios or merge if scope too narrow` |

#### 5.3 New Sub-Principle -- Scenario Ordering Guideline

**Location:** After SD-05 (after line 106) and before SD-06.

**Add new section SD-05a (as a sub-principle under SD-05, to avoid renumbering SD-06 through SD-09):**

```markdown
### SD-05a: Scenario Ordering (TPP-Based)

Gherkin scenarios within each story MUST be ordered following the Transformation Priority Premise (TPP):

| Order | Category | Rationale |
|-------|----------|-----------|
| 1 | Degenerate cases | Null, empty, zero — simplest transformations, fail-fast guards |
| 2 | Happy path (basic) | Single successful flow with minimal valid input |
| 3 | Happy path (variations) | Alternate valid inputs, optional fields present |
| 4 | Error paths | Business rule violations, validation failures |
| 5 | Boundary values | At-min, at-max, past-max for each bounded input |
| 6 | Complex edge cases | Combinations, race conditions, state transitions |

**Rationale:** TPP ordering ensures the implementation grows incrementally from simple to complex, matching the natural TDD red-green-refactor cycle.
```

**Alternative placement consideration:** This could also be numbered SD-10 to maintain clean numbering. The choice between SD-05a (logically grouped with sizing) and SD-10 (clean sequential numbering) is a style decision. SD-05a is recommended because it is semantically a sub-rule of scenario management, not a fully independent principle.

#### 5.4 Anti-Patterns Addition

**Location:** Anti-Patterns section at the end of the file.

**Add new anti-patterns:**

```markdown
- Story with fewer than 4 Gherkin scenarios → **FORBIDDEN** (add degenerate/boundary/error)
- Gherkin scenarios without degenerate cases (null, empty, zero) → **FORBIDDEN**
- Boundary-dependent behavior without triplet scenarios (at-min, at-max, past-max) → **FORBIDDEN**
- Gherkin scenarios ordered happy-path-first (degenerate cases must come first) → **REORDER**
```

---

### 6. Golden File Update Strategy

#### Strategy: Copy-After-Edit

1. Edit the source of truth (`resources/core/13-story-decomposition.md`)
2. Copy the edited file to all 16 golden file locations
3. Run the byte-for-byte test suite to verify parity

#### Mechanical execution:

```bash
# After editing the source of truth:
SOURCE="resources/core/13-story-decomposition.md"
PROFILES=(go-gin java-quarkus java-spring kotlin-ktor python-click-cli python-fastapi rust-axum typescript-nestjs)
for profile in "${PROFILES[@]}"; do
  cp "$SOURCE" "tests/golden/$profile/.claude/skills/story-planning/references/story-decomposition.md"
  cp "$SOURCE" "tests/golden/$profile/.agents/skills/story-planning/references/story-decomposition.md"
done
```

#### Verification:

```bash
npx vitest run tests/node/integration/byte-for-byte.test.ts
```

All 8 profiles x 5 test assertions = 40 test cases should pass (pipeline success, golden match, no missing files, no extra files, total files > 0).

---

### 7. Implementation Order

| Step | Action | Verification |
|------|--------|-------------|
| 1 | Edit `resources/core/13-story-decomposition.md` -- add SD-02 Gherkin completeness sub-section | Manual review of markdown structure |
| 2 | Edit `resources/core/13-story-decomposition.md` -- update SD-05 minimum table (2 -> 4) | Manual review |
| 3 | Edit `resources/core/13-story-decomposition.md` -- add SD-05a scenario ordering guideline | Manual review |
| 4 | Edit `resources/core/13-story-decomposition.md` -- add anti-patterns | Manual review |
| 5 | Copy source to all 16 golden files | `diff` confirmation |
| 6 | Run byte-for-byte test suite | All 40 assertions pass |
| 7 | Run full test suite | All 1,384+ tests pass, coverage >= 95% line / >= 90% branch |

---

### 8. Risk Assessment

| Risk | Severity | Probability | Mitigation |
|------|----------|-------------|------------|
| Golden file mismatch after edit | HIGH | LOW | Mechanical copy script (Section 6) eliminates drift. Run byte-for-byte tests immediately. |
| Breaking existing SD-01 through SD-09 numbering | MEDIUM | LOW | Use SD-05a sub-numbering instead of inserting new SD-XX that shifts existing numbers. All references to SD-06..SD-09 remain valid. |
| Stale generated copies in workspace (`skills/`, `.claude/`, `.agents/`) confuse developers | LOW | MEDIUM | These files are already stale (missing SD-09, different SD-07). They are generated output, not committed source. Document that `ia-dev-env generate` must be re-run to refresh them. |
| Markdown rendering issues with nested sub-sections in SD-02 | LOW | LOW | Test rendering in a Markdown previewer before committing. |
| Scope creep -- temptation to also update the `story-planning/SKILL.md` description | LOW | MEDIUM | SKILL.md description already says "sizing rules" generically. No update needed. Keep changes minimal. |
| Conflict with story-0003-0003 (if it also modifies Rule 13) | MEDIUM | LOW | story-0003-0003 targets a different rule or different section. Check dependency DAG: story-0003-0004 depends only on story-0003-0001. |

---

### 9. Backward Compatibility

This change is **purely additive**:
- No existing SD-XX principles are removed or renumbered
- SD-02 gains a new sub-section (existing content preserved verbatim)
- SD-05 minimum changes from 2 to 4 (tightening, not relaxing -- existing compliant stories remain compliant since 4 > 2)
- New SD-05a is inserted between existing SD-05 and SD-06 without renumbering
- New anti-patterns are appended to existing list

Stories previously generated with >= 4 scenarios remain valid. Stories with exactly 2-3 scenarios would now be flagged as below-minimum, which is the intended behavior change.

---

### 10. Out of Scope

- Modifying `src/domain/core-kp-routing.ts` (no filename change)
- Modifying `src/assembler/rules-assembler.ts` (no pipeline logic change)
- Modifying test infrastructure (`byte-for-byte.test.ts`, `integration-constants.ts`)
- Updating `skills/story-planning/SKILL.md` (description remains accurate)
- Updating the 3 workspace-generated copies (`skills/`, `.claude/`, `.agents/`) -- these are regenerated by the pipeline
