# História: `Agent(...)` com `model:` em x-arch-plan e x-test-plan

**ID:** story-0050-0006
**Chave Jira:** —
**Status:** Pendente

## 1. Dependências

| Blocked By | Blocks |
| :--- | :--- |
| story-0050-0005 | story-0050-0009 |

## 2. Regras Transversais Aplicáveis

| ID | Título |
| :--- | :--- |
| RULE-002 | `Agent(...)` com `model:` explícito |
| RULE-007 | Backward compatibility (escopo aditivo) |

## 3. Descrição

Como **engenheiro de plataforma**, eu quero aplicar o padrão estabelecido na STORY-0050-0005 às invocações `Agent(...)` dos skills de planejamento `x-arch-plan` e `x-test-plan`, para fechar as menções remanescentes de "RULE-009" que vivem em prosa nesses dois arquivos.

### 3.1 Localização das invocações

**`x-arch-plan/SKILL.md`** (367 linhas) — 2 pontos de menção `model: opus` em prosa:
- Linha ~153: "Launch subagent with `model: opus` for deep architectural reasoning"
- Linha ~255: idem

**`x-test-plan/SKILL.md`** (314 linhas) — 3 pontos de menção:
- Linhas ~36, ~57, ~310: "Subagent uses `model: opus` (RULE-009) for deep test planning"

### 3.2 Mudança exata

Cada invocação `Agent(...)` nesses dois arquivos passa `model: "opus"` explicitamente — planning arquitetural e planning de testes (especialmente Double-Loop TDD + TPP) justificam Opus. Remover comentários em prosa que ficaram redundantes.

Formato final igual ao Pattern 2 de Rule 13, consistente com STORY-0050-0005.

## 3.5 Entrega de Valor

- **Valor Principal:** Fecha os últimos 5 pontos de menção "RULE-009" em prosa, convertendo-os em enforcement técnico. Torna o audit (STORY-0050-0009) capaz de passar para esses 2 skills.
- **Métrica de Sucesso:** Todas invocações `Agent(...)` em x-arch-plan e x-test-plan têm `model: "opus"`; `grep "RULE-009.*prose"` ou "model hint" retorna 0 matches.
- **Impacto no Negócio:** Complementa a STORY-0050-0005; sozinha tem impacto menor (esses skills são invocados menos frequentemente), mas necessária para completude do enforcement.

## 4. Definições de Qualidade Locais

### DoR Local

- [ ] STORY-0050-0005 mergeada (padrão estabelecido e validado)

### DoD Local

- [ ] Todas invocações `Agent(...)` em x-arch-plan e x-test-plan têm `model: "opus"`
- [ ] Comentários "model hint" / "RULE-009" em prosa removidos
- [ ] Golden files regenerados
- [ ] Audit Rule 23 Check B passa para esses 2 skills

## 5. Contratos de Dados

2 SKILL.md modificados com ~5 hunks de `+1 linha model:` cada.

## 6. Diagramas

N/A — análogo ao diagrama de STORY-0050-0005.

## 7. Critérios de Aceite (Gherkin)

```gherkin
Cenário: x-arch-plan subagent usa Opus
  DADO x-arch-plan/SKILL.md gerado
  QUANDO uma invocação Agent() é lida
  ENTÃO contém 'model: "opus"'

Cenário: x-test-plan subagent usa Opus
  DADO x-test-plan/SKILL.md gerado
  QUANDO uma invocação Agent() é lida
  ENTÃO contém 'model: "opus"'

Cenário: Prosa "RULE-009" foi substituída por enforcement
  DADO os 2 SKILL.md gerados
  QUANDO grepados por "model hint"
  ENTÃO 0 matches

Cenário: Boundary — todas menções viram declaração formal
  DADO os 5 pontos originais (2 em arch-plan + 3 em test-plan)
  QUANDO a mudança é aplicada
  ENTÃO 5 invocações Agent() têm "model:"
```

### 7.1 Scenario Ordering (TPP)

Degenerate (x-arch-plan só) → happy (ambos) → invariance (prosa removida) → boundary (5 pontos fechados).

### 7.2 Mandatory Scenario Categories

- [x] Degenerate
- [x] Happy path
- [x] Error paths (ausência = erro)
- [x] Boundary

## 8. Tasks

### TASK-0050-0006-001: Refatorar invocações em x-arch-plan

- **Layer:** Doc
- **Test Type:** Verification
- **Size:** M
- **Dependencies:** —
- **Branch:** `feat/task-0050-0006-001-arch-plan`
- **Testability:** INDEPENDENT
- **Files:**
  - `java/src/main/resources/targets/claude/skills/core/x-arch-plan/SKILL.md`
- **Acceptance Criteria:**
  - [ ] 2 invocações Agent() com `model: "opus"` explícito
  - [ ] Prose hints removidas
  - [ ] Pattern 2 da Rule 13 aplicado

### TASK-0050-0006-002: Refatorar invocações em x-test-plan

- **Layer:** Doc
- **Test Type:** Verification
- **Size:** M
- **Dependencies:** TASK-0050-0006-001
- **Branch:** `feat/task-0050-0006-002-test-plan`
- **Testability:** INDEPENDENT
- **Files:**
  - `java/src/main/resources/targets/claude/skills/core/x-test-plan/SKILL.md`
- **Acceptance Criteria:**
  - [ ] 3 invocações Agent() com `model: "opus"`
  - [ ] Prose hints removidas

### TASK-0050-0006-003: Regenerar goldens + audit preview

- **Layer:** Test
- **Test Type:** Integration
- **Size:** S
- **Dependencies:** TASK-0050-0006-002
- **Branch:** `feat/task-0050-0006-003-goldens`
- **Testability:** INDEPENDENT
- **Files:**
  - `src/test/resources/golden/**`
- **Acceptance Criteria:**
  - [ ] `mvn verify` ok
  - [ ] 5 matches `model: "opus"` nos 2 arquivos
  - [ ] 0 matches `model hint` (grep)
