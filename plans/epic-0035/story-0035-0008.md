# História: Regenerar Golden Files, Atualizar Testes Java e Documentar Skill Estendido

**ID:** story-0035-0008
**Chave Jira:** —
**Status:** Pendente

## 1. Dependências

| Blocked By | Blocks |
| :--- | :--- |
| story-0035-0001, story-0035-0002, story-0035-0003, story-0035-0004, story-0035-0005, story-0035-0006, story-0035-0007 | — |

## 2. Regras Transversais Aplicáveis

| ID | Título |
| :--- | :--- |
| RULE-005 | Source of Truth (SKILL Edit Exclusivo em `targets/`) |
| RULE-006 | Coverage Não Pode Degradar |
| RULE-007 | Conventional Commits + Rule 08 |

## 3. Descrição

Como **maintainer do `ia-dev-env`**, eu quero que toda a documentação do skill atualizado (`SKILL.md`, `README.md`, `references/*.md`), os golden files dos 17+ profiles, os testes Java (existentes atualizados + novo `ReleaseStateFileSchemaTest`) e o CHANGELOG do próprio projeto reflitam o novo comportamento do `x-release` de forma consistente, garantindo zero drift entre source of truth, artefatos gerados e documentação consumida por novos contribuidores.

Esta é a **story de consolidação final**. Depende de todas as anteriores porque regenerar golden files requer que o `SKILL.md` esteja no estado final completo, e os testes Java precisam ter o comportamento completo para validar. Escopo: (1) regenerar golden files nos 17+ profiles após todas as mudanças em `SKILL.md`, `README.md` e `references/`; (2) atualizar os 3 testes Java existentes (`ReleaseSkillTest`, `ReleaseChecklistAssemblerTest`, `ReleaseManagementGitFlowTest`) para validar o novo contrato; (3) criar teste novo `ReleaseStateFileSchemaTest` que valida schema JSON formal do state file conforme definido na story 0035-0001; (4) reescrever `README.md` do skill refletindo o fluxo novo com exemplos; (5) consolidar/finalizar `references/approval-gate-workflow.md`, `references/state-file-schema.md`, `references/backmerge-strategies.md`; (6) adicionar entry em `[Unreleased]` do `CHANGELOG.md` do `ia-dev-env`; (7) ajustar `.claude/rules/08-release-process.md` se necessário para refletir a nova expectativa de approval gate.

### 3.1 Regeneração de Golden Files

Após todas as mudanças em `java/src/main/resources/targets/claude/skills/core/x-release/**`, regenerar golden files:

```bash
cd java

# Pré-requisito (feedback de memória do projeto)
mvn process-resources

# Regenerar golden files
mvn test-compile exec:java \
  -Dexec.mainClass="dev.iadev.golden.GoldenFileRegenerator" \
  -Dexec.classpathScope=test
```

Os 17+ profiles a serem regenerados (os testes de golden validam saídas em `.claude/`, `.agents/`, `.codex/` e `.github/` enquanto o epic-0034 não for merged):
- go-gin, java-quarkus, java-spring, java-spring-clickhouse, java-spring-cqrs-es, java-spring-elasticsearch, java-spring-event-driven, java-spring-fintech-pci, java-spring-hexagonal, java-spring-neo4j, kotlin-ktor, python-click-cli, python-fastapi, python-fastapi-timescale, rust-axum, typescript-commander-cli, typescript-nestjs

**Nota importante sobre impacto dos golden files:** a contagem de arquivos depende do estado do epic-0034 (remoção de targets não-Claude) quando esta story for implementada:

- **Se epic-0034 já estiver merged** (apenas `.claude/` ativo): 5 arquivos × 17 profiles = **85 arquivos golden**
- **Se epic-0034 ainda estiver pendente** (targets `.claude/`, `.agents/`, `.codex/`, `.github/` todos ativos): 5 arquivos × 4 targets × 17 profiles = **340 arquivos golden**

Arquivos afetados em cada profile (por target diretório ativo):
- `SKILL.md` (major rewrite)
- `README.md` (rewrite)
- `references/approval-gate-workflow.md` (novo)
- `references/state-file-schema.md` (novo)
- `references/backmerge-strategies.md` (novo)

Rodar `git diff --stat` após a regeneração para confirmar a contagem real de arquivos afetados.

### 3.2 Atualização de Testes Java Existentes

#### 3.2.1 `ReleaseSkillTest.java`

Atualizar assertions sobre:
- Frontmatter: novo `description`, `allowed-tools` com `Skill, AskUserQuestion`, `argument-hint` com 4 novas flags
- Parameters table: 4 linhas novas (`--continue-after-merge`, `--interactive`, `--signed-tag`, `--state-file`)
- Presença de seções: Step 0, Step 2 (VALIDATE-DEEP), Step 7 (OPEN-RELEASE-PR), Step 8 (APPROVAL-GATE), Step 9 (RESUME-AND-TAG), Step 10 (BACK-MERGE-DEVELOP)
- Ausência de merge direto para main/develop em Step 7 e Step 9 antigos

#### 3.2.2 `ReleaseChecklistAssemblerTest.java`

Atualizar checklist atual para incluir:
- "Approval gate PR merged via GitHub UI (not local git merge)"
- "State file deleted after successful release"
- "CHANGELOG [X.Y.Z] entry matches PR body"

#### 3.2.3 `ReleaseManagementGitFlowTest.java`

Ajustar assertions sobre:
- `git merge` é substituído por `gh pr create` para main e develop
- Hotfix workflow usa PR-flow
- Back-merge preserva SNAPSHOT advance Java

### 3.3 Novo Teste: `ReleaseStateFileSchemaTest.java`

```java
package dev.iadev.application.assembler;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.Test;
import java.nio.file.Path;
import java.nio.file.Files;
import java.util.List;
import static org.assertj.core.api.Assertions.assertThat;

class ReleaseStateFileSchemaTest {

    private static final Path SKILL_PATH = Path.of(
        "src/main/resources/targets/claude/skills/core/x-release/SKILL.md");
    private static final Path SCHEMA_DOC_PATH = Path.of(
        "src/main/resources/targets/claude/skills/core/x-release/references/state-file-schema.md");

    private static final List<String> REQUIRED_PHASES = List.of(
        "INITIALIZED", "DETERMINED", "VALIDATED", "BRANCHED", "UPDATED",
        "CHANGELOG_DONE", "COMMITTED", "PR_OPENED", "APPROVAL_PENDING",
        "MERGED", "TAGGED", "BACKMERGE_OPENED", "BACKMERGE_CONFLICT", "COMPLETED"
    );

    // Fields always present in the state file, from INITIALIZED onwards.
    private static final List<String> ALWAYS_PRESENT_FIELDS = List.of(
        "schemaVersion", "version", "phase", "branch", "baseBranch",
        "hotfix", "dryRun", "signedTag", "interactive",
        "startedAt", "lastPhaseCompletedAt", "phasesCompleted",
        "targetVersion", "previousVersion", "bumpType"
    );

    // Fields populated only after specific phases:
    //   prNumber/prUrl/prTitle  -> after PR_OPENED
    //   changelogEntry          -> after CHANGELOG_DONE
    //   tagMessage              -> after TAGGED
    private static final List<String> PHASE_DEPENDENT_FIELDS = List.of(
        "prNumber", "prUrl", "prTitle", "changelogEntry", "tagMessage"
    );

    @Test
    void stateFileSchemaDocExists() throws Exception {
        assertThat(SCHEMA_DOC_PATH).as("references/state-file-schema.md must exist").exists();
    }

    @Test
    void schemaDocMentionsAllRequiredPhases() throws Exception {
        String content = Files.readString(SCHEMA_DOC_PATH);
        assertThat(content).as("Schema doc must mention every phase").contains(REQUIRED_PHASES);
    }

    @Test
    void schemaDocMentionsAllFields() throws Exception {
        String content = Files.readString(SCHEMA_DOC_PATH);
        assertThat(content).as("Schema doc must mention every always-present field")
            .contains(ALWAYS_PRESENT_FIELDS);
        assertThat(content).as("Schema doc must mention every phase-dependent field")
            .contains(PHASE_DEPENDENT_FIELDS);
    }

    @Test
    void exampleJsonInSchemaDocIsValid() throws Exception {
        String content = Files.readString(SCHEMA_DOC_PATH);
        // Extract JSON fenced block — start can be 0 when the doc opens with it.
        int start = content.indexOf("```json");
        int end = content.indexOf("```", start + 7);
        assertThat(start).as("JSON fence must be present").isGreaterThanOrEqualTo(0);
        assertThat(end).as("JSON fence must be closed after start").isGreaterThan(start);
        String json = content.substring(start + 7, end).trim();

        ObjectMapper mapper = new ObjectMapper();
        JsonNode node = mapper.readTree(json);

        assertThat(node.get("schemaVersion").asInt()).isEqualTo(1);
        assertThat(REQUIRED_PHASES).contains(node.get("phase").asText());
        for (String field : ALWAYS_PRESENT_FIELDS) {
            assertThat(node.has(field)).as("Example JSON missing always-present field: " + field).isTrue();
        }
        // Phase-dependent fields are optional in the example; do not assert their presence.
    }

    @Test
    void skillMdReferencesStateFileSchema() throws Exception {
        String content = Files.readString(SKILL_PATH);
        assertThat(content).as("SKILL.md must reference the schema doc")
            .contains("state-file-schema.md");
        assertThat(content).as("SKILL.md must mention schemaVersion field")
            .contains("schemaVersion");
    }

    @Test
    void skillMdDefinesPhaseTransitions() throws Exception {
        String content = Files.readString(SKILL_PATH);
        List<String> transitions = List.of(
            "INITIALIZED", "VALIDATED", "PR_OPENED",
            "APPROVAL_PENDING", "MERGED", "TAGGED", "BACKMERGE_OPENED"
        );
        assertThat(content).as("SKILL.md must define all happy-path phases").contains(transitions);
    }
}
```

### 3.4 Reescrita do `README.md` do Skill

Criar novo `java/src/main/resources/targets/claude/skills/core/x-release/README.md`:

```markdown
# x-release Skill

Orchestrates Git Flow release with approval gate and PR-flow.

## Quick Start

### Standard release (minor bump)
```
/x-release minor
# ... skill runs phases 0-8 ...
# ... halts at APPROVAL-GATE, prints PR URL ...

# [User merges PR in GitHub UI]

/x-release 2.3.0 --continue-after-merge
# ... skill runs phases 9-11, tags main, opens back-merge PR ...
```

### Hotfix release
```
/x-release --hotfix
# ... same flow but from main, patch only ...
```

### Dry-run (preview without changes)
```
/x-release minor --dry-run
```

### Interactive mode (no halt, inline pause)
```
/x-release minor --interactive
# ... skill uses AskUserQuestion to pause instead of exit ...
```

## Phases

| # | Phase | Description | Halts? |
|---|---|---|---|
| 0 | RESUME_DETECTION | Load/create state, verify deps | — |
| 1 | DETERMINE | Parse bump type, compute version | — |
| 2 | VALIDATE_DEEP | 8 checks (tests, coverage, golden, consistency) | — |
| 3 | BRANCH | Create release/* or hotfix/* | — |
| 4 | UPDATE | Update version in pom.xml etc. | — |
| 5 | CHANGELOG | Delegate to x-release-changelog | — |
| 6 | COMMIT | Create release commit | — |
| 7 | OPEN_RELEASE_PR | gh pr create --base main | — |
| **8** | **APPROVAL_GATE** | **Persist state, halt** | **✋ HUMAN** |
| 9 | RESUME_AND_TAG | Verify merged, tag main | — |
| 10 | BACK_MERGE_DEVELOP | gh pr create --base develop | — |
| 11 | CLEANUP | Delete branch and state file | — |

## State File

The skill persists its progress in `plans/release-state-<X.Y.Z>.json`.
See `references/state-file-schema.md` for the full schema.

## References

- `references/approval-gate-workflow.md` — How the human approval pause works
- `references/state-file-schema.md` — State file JSON schema and phase transitions
- `references/backmerge-strategies.md` — Clean vs conflict back-merge handling

## Dependencies

- `gh` CLI ≥ 2.0 (authenticated via `gh auth login`)
- `jq` for JSON parsing
- `git` ≥ 2.30

## See Also

- `x-release-changelog` — invoked in Phase 5
- `x-git-push` — Conventional Commits reference
- `x-review-pr` — optional integration in Phase 7
```

### 3.5 CHANGELOG Entry

Adicionar em `CHANGELOG.md` na raiz do projeto, dentro de `[Unreleased]`:

```markdown
## [Unreleased]

### Added
- EPIC-0035: `x-release` skill extended with approval gate, PR-flow, and deep validation
  - New flags: `--continue-after-merge`, `--interactive`, `--signed-tag`, `--state-file`
  - State persistence in `plans/release-state-<X.Y.Z>.json` with versioned JSON schema
  - New references: `approval-gate-workflow.md`, `state-file-schema.md`, `backmerge-strategies.md`
- EPIC-0035: New template variables `{{COVERAGE_LINE_THRESHOLD}}`, `{{COVERAGE_BRANCH_THRESHOLD}}`, `{{GOLDEN_TEST_COMMAND}}`, `{{GENERATION_COMMAND}}` in `ContextBuilder`

### Changed
- EPIC-0035: `x-release` Phase VALIDATE-DEEP now runs 8 checks (tests, coverage, golden files, hardcoded version strings, cross-file consistency) in addition to the basic build. Flag `--skip-tests` only skips tests 4-6, not the consistency checks.
- EPIC-0035: `x-release` Phase OPEN-RELEASE-PR replaces direct `git merge` to main with `gh pr create --base main` + human approval gate
- EPIC-0035: `x-release` Phase BACK-MERGE-DEVELOP replaces direct `git merge` to develop with `gh pr create --base develop` + conflict detection
- EPIC-0035: Hotfix workflow updated to use PR-flow while preserving all existing semantics (patch only, back-merge to develop + active release/*)

### Fixed
- EPIC-0035: `x-release` no longer violates Rule 09 (Branching Model) — all merges to `main` and `develop` now go through PRs
```

### 3.6 Ajuste (se necessário) em Rule 08

Revisar `.claude/rules/08-release-process.md`. Se o checklist atual não menciona approval gate ou PR-flow, adicionar:

```markdown
## Release Checklist (updated)

- [ ] All tests passing on release branch
- [ ] Coverage thresholds met (Rule 05)
- [ ] CHANGELOG.md updated
- [ ] Version bumped according to SemVer
- [ ] No TODO/FIXME/HACK comments in release scope
- [ ] Security scan clean
- [ ] Rollback plan documented
- [ ] **Approval gate PR merged via GitHub UI (NOT local git merge)** ← new
- [ ] **State file cleaned up after successful release** ← new
```

Nota: esta regra é gerada a partir de `java/src/main/resources/targets/claude/rules/08-release-process.md` — editar apenas o source of truth, não o gerado.

## 3.5 Entrega de Valor

- **Valor Principal:** Zero drift entre código do skill, golden files, documentação e testes. Novos contribuidores encontram um `README.md` claro com quickstart, um schema JSON formal para entender state persistence, e testes automatizados que falham em qualquer inconsistência entre doc e implementação.
- **Métrica de Sucesso:** `mvn verify -Pall-tests` verde com 85 golden files atualizados + `ReleaseStateFileSchemaTest` novo passando + 3 testes Java existentes atualizados passando. Coverage permanece ≥ 95% line / ≥ 90% branch. CHANGELOG.md `[Unreleased]` tem entry de EPIC-0035.
- **Impacto no Negócio:** O skill fica "shippable" — pronto para ser usado em releases reais do `ia-dev-env` e propagado para projetos downstream via `ia-dev-env generate`. Time ganha confiança ao ver que a documentação está sincronizada com o comportamento.

## 4. Definições de Qualidade Locais

### DoR Local

- [ ] Stories 0035-0001 a 0035-0007 merged em develop
- [ ] `mvn process-resources` e `GoldenFileRegenerator` identificados como ferramentas canônicas
- [ ] Lista dos 17+ profiles conhecida
- [ ] `ReleaseSkillTest.java`, `ReleaseChecklistAssemblerTest.java`, `ReleaseManagementGitFlowTest.java` lidos
- [ ] Baseline de coverage registrada antes da story começar

### DoD Local

- [ ] 85 golden files (17 profiles × 5 arquivos) regenerados
- [ ] `ReleaseSkillTest` atualizado com novas assertions e verde
- [ ] `ReleaseChecklistAssemblerTest` atualizado com 3 items novos e verde
- [ ] `ReleaseManagementGitFlowTest` atualizado para validar PR-flow e verde
- [ ] `ReleaseStateFileSchemaTest` novo criado com 6+ testes
- [ ] `README.md` do skill reescrito com quickstart, phases table, dependências
- [ ] `references/approval-gate-workflow.md`, `references/state-file-schema.md`, `references/backmerge-strategies.md` finalizados e consistentes
- [ ] `CHANGELOG.md` raiz com entry de EPIC-0035 em `[Unreleased]`
- [ ] `.claude/rules/08-release-process.md` (source of truth) ajustado se necessário
- [ ] `mvn clean verify -Pall-tests` verde
- [ ] JaCoCo report mostra coverage ≥ 95% line / ≥ 90% branch (sem degradação > 1pp vs baseline)
- [ ] Commit atômico ou série de commits em `feature/story-0035-0008-consolidation`
- [ ] PR aberto contra develop com screenshot/diff do output esperado do dry-run

## 5. Contratos de Dados

### 5.1 Golden Files Matrix

| Profile | Files Affected |
|---|---|
| go-gin, java-quarkus, java-spring, ..., typescript-nestjs (17 total) | SKILL.md, README.md, approval-gate-workflow.md, state-file-schema.md, backmerge-strategies.md |

Total: 17 × 5 = **85 golden files** criados/modificados.

### 5.2 Test Coverage Delta

| Test | Assertions Added | New Lines |
|---|---|---|
| `ReleaseSkillTest` | ~10 | ~30 |
| `ReleaseChecklistAssemblerTest` | ~3 | ~10 |
| `ReleaseManagementGitFlowTest` | ~6 | ~20 |
| `ReleaseStateFileSchemaTest` (novo) | 6 testes | ~100 |

Total: ~160 linhas de código de teste novo.

### 5.3 CHANGELOG Entry Format

A entry para EPIC-0035 DEVE seguir Keep a Changelog format:
- Subseção `### Added` para itens novos (flags, schema, references)
- Subseção `### Changed` para modificações de comportamento
- Subseção `### Fixed` para o fix da Rule 09

Cada item DEVE começar com `EPIC-0035:` para rastreabilidade.

## 6. Diagramas

### 6.1 Dependências desta Story

```mermaid
graph TD
    S1[story-0001: Foundation]
    S2[story-0002: VALIDATE-DEEP]
    S3[story-0003: OPEN-RELEASE-PR]
    S4[story-0004: APPROVAL-GATE]
    S5[story-0005: RESUME-AND-TAG]
    S6[story-0006: BACK-MERGE-DEVELOP]
    S7[story-0007: Dry-run + Errors + Hotfix]
    S8[story-0008: Golden + Tests + Docs (THIS)]

    S1 --> S8
    S2 --> S8
    S3 --> S8
    S4 --> S8
    S5 --> S8
    S6 --> S8
    S7 --> S8

    classDef current fill:#e94560,color:#fff
    class S8 current
```

## 7. Critérios de Aceite (Gherkin)

```gherkin
Cenario: Degenerate — golden file test fails quando SKILL.md é editado sem regenerar
  DADO que SKILL.md do x-release foi editado
  E golden files não foram regenerados
  QUANDO mvn verify -Pall-tests é executado
  ENTÃO pelo menos um *GoldenTest falha
  E a mensagem indica qual profile está defasado

Cenario: Happy path — regeneração completa dos golden files
  DADO que todas as mudanças das stories 0001-0007 foram aplicadas ao source of truth
  QUANDO mvn process-resources && GoldenFileRegenerator é executado
  ENTÃO 85 arquivos golden (5 × 17 profiles) são atualizados
  E mvn verify -Pall-tests passa sem falhas
  E git diff mostra apenas mudanças em java/src/test/resources/golden/*/.claude/skills/x-release/

Cenario: Happy path — ReleaseStateFileSchemaTest valida consistência
  DADO que references/state-file-schema.md existe com JSON de exemplo
  E SKILL.md referencia o schema doc
  QUANDO ReleaseStateFileSchemaTest é executado
  ENTÃO todos os 6 testes passam
  E o exemplo JSON é parse-able
  E contém todos os 20 campos obrigatórios
  E contém todas as 14 phases do enum

Cenario: Error — teste existente ReleaseSkillTest falha por assertion desatualizada
  DADO que ReleaseSkillTest tem assertion "git merge main"
  QUANDO o teste é executado contra o novo SKILL.md
  ENTÃO a assertion falha (não existe mais "git merge main")
  E o desenvolvedor precisa atualizar a assertion para "gh pr create --base main"

Cenario: Happy path — CHANGELOG.md tem entry EPIC-0035 completa
  DADO que CHANGELOG.md foi atualizado
  QUANDO o parser de CHANGELOG lê a seção [Unreleased]
  ENTÃO encontra 3 subseções: Added, Changed, Fixed
  E cada subseção tem pelo menos 1 item prefixado "EPIC-0035:"
  E a subseção Fixed menciona "no longer violates Rule 09"

Cenario: Boundary — coverage exatamente no threshold após adições
  DADO que testes novos foram adicionados
  E o código do skill não mudou (apenas markdown)
  QUANDO JaCoCo report é gerado
  ENTÃO line coverage >= 95.00%
  E branch coverage >= 90.00%
  E a degradação vs baseline pré-story é <= 1pp
```

### 7.1 Scenario Ordering (TPP)

Degenerate (missing regen) → happy (regen works) → happy (schema test) → error (outdated assertion) → happy (CHANGELOG) → boundary (coverage).

### 7.2 Mandatory Scenario Categories

- [x] Degenerate (golden drift detected)
- [x] Happy path (regen, schema test, CHANGELOG)
- [x] Error paths (outdated assertion)
- [x] Boundary (coverage at threshold)

## 8. Tasks

### TASK-0035-0008-001: Regenerar golden files nos 17+ profiles

- **Layer:** Test
- **Test Type:** Smoke
- **Size:** M
- **Dependencies:** —
- **Branch:** `feature/task-0035-0008-001-golden-regen`
- **Testability:** Migration + Smoke
- **Files:**
  - `java/src/test/resources/golden/*/.claude/skills/x-release/SKILL.md` (17 files)
  - `java/src/test/resources/golden/*/.claude/skills/x-release/README.md` (17 files)
  - `java/src/test/resources/golden/*/.claude/skills/x-release/references/approval-gate-workflow.md` (17 files)
  - `java/src/test/resources/golden/*/.claude/skills/x-release/references/state-file-schema.md` (17 files)
  - `java/src/test/resources/golden/*/.claude/skills/x-release/references/backmerge-strategies.md` (17 files)
- **Acceptance Criteria:**
  - [ ] `mvn process-resources` executado antes da regeneração
  - [ ] `GoldenFileRegenerator` executado com sucesso
  - [ ] 85 arquivos atualizados (conferir via `git diff --stat`)
  - [ ] `mvn verify -Pall-tests` passa em todos os 17 profiles
  - [ ] Diff visual inspecionado em 2-3 profiles amostra

### TASK-0035-0008-002: Atualizar 3 testes Java existentes + criar ReleaseStateFileSchemaTest

- **Layer:** Test
- **Test Type:** Unit + Integration
- **Size:** M
- **Dependencies:** TASK-0035-0008-001
- **Branch:** `feature/task-0035-0008-002-java-tests`
- **Testability:** Domain + UnitTest
- **Files:**
  - `java/src/test/java/dev/iadev/application/assembler/ReleaseSkillTest.java` (edit)
  - `java/src/test/java/dev/iadev/application/assembler/ReleaseChecklistAssemblerTest.java` (edit)
  - `java/src/test/java/dev/iadev/application/assembler/ReleaseManagementGitFlowTest.java` (edit)
  - `java/src/test/java/dev/iadev/application/assembler/ReleaseStateFileSchemaTest.java` (novo)
- **Acceptance Criteria:**
  - [ ] `ReleaseSkillTest` valida novo frontmatter, novas flags, novas phases
  - [ ] `ReleaseChecklistAssemblerTest` tem 3 items novos no checklist
  - [ ] `ReleaseManagementGitFlowTest` valida PR-flow e ausência de `git merge` direto
  - [ ] `ReleaseStateFileSchemaTest` novo tem 6+ testes (ver Section 3.3)
  - [ ] Todos os 4 testes verdes
  - [ ] Coverage não degrada

### TASK-0035-0008-003: Reescrever README.md do skill, finalizar references/, atualizar CHANGELOG, ajustar Rule 08

- **Layer:** Doc
- **Test Type:** Verification
- **Size:** M
- **Dependencies:** TASK-0035-0008-001, TASK-0035-0008-002
- **Branch:** `feature/task-0035-0008-003-docs-final`
- **Testability:** Config + VerificationTest
- **Files:**
  - `java/src/main/resources/targets/claude/skills/core/x-release/README.md` (rewrite)
  - `java/src/main/resources/targets/claude/skills/core/x-release/references/approval-gate-workflow.md` (finalize)
  - `java/src/main/resources/targets/claude/skills/core/x-release/references/state-file-schema.md` (finalize)
  - `java/src/main/resources/targets/claude/skills/core/x-release/references/backmerge-strategies.md` (finalize)
  - `CHANGELOG.md` (root)
  - `java/src/main/resources/targets/claude/rules/08-release-process.md` (if needed)
- **Acceptance Criteria:**
  - [ ] README.md tem Quick Start com 3 exemplos (standard, hotfix, dry-run, interactive)
  - [ ] README.md tem tabela de phases com marcador HALT em Phase 8
  - [ ] README.md lista dependências (gh, jq, git)
  - [ ] `references/*.md` finalizados — sem TODOs
  - [ ] `CHANGELOG.md` `[Unreleased]` tem subseções Added/Changed/Fixed com itens EPIC-0035
  - [ ] Se Rule 08 foi ajustada, golden files foram regenerados novamente
  - [ ] `mvn clean verify -Pall-tests` verde
  - [ ] Story 0035-0008 PR aberto contra develop com link para story
