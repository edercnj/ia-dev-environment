# STORY-0018-007: Docs — Atualizar README.md, CLAUDE.md + Remover docs/ Vazio

> **Epic**: EPIC-0018
> **Camada**: COMPOSITION
> **Sizing**: S
> **Status**: DRAFT

---

## 1. DEPENDENCY TABLE

| Tipo | Story ID | Titulo | Status esperado |
|---|---|---|---|
| Blocked by | STORY-0018-002 | Steering: criar arquivos base | DONE |
| Blocked by | STORY-0018-006 | Java: atualizar assemblers + skill templates | DONE |
| Blocks | -- | -- | -- |

## 2. APPLICABLE RULES

- RULE-003 — AGENTS.md e CLAUDE.md permanecem funcionais durante a transição

---

## 3. DESCRIPTION

### State Before

- `README.md` documenta a estrutura com `docs/` como diretório principal de documentação
- `CLAUDE.md` referencia `docs/stories/`, `docs/specs/`, etc. na explicação do projeto
- `docs/` existe mas está vazio (todo conteúdo migrado nas stories 002-005)
- `docs/.DS_Store` pode existir (artifact do macOS)

### State After

- `README.md` documenta a nova estrutura SDD com `steering/`, `specs/`, `plans/`, `results/`, `contracts/`, `adr/`
- `CLAUDE.md` referencia os novos paths e descreve o fluxo SDD
- `docs/` removido (incluindo .DS_Store se existir)
- Nenhuma referência a `docs/` como diretório de documentação permanece nos arquivos raiz

### O que esta story NAO faz

- Não modifica AGENTS.md (gerado — será atualizado na próxima regeneração)
- Não modifica `.codex/` ou `.agents/` (gerados)
- Não modifica `.claude/rules/` (referências a skills paths são internas ao gerador)
- Não modifica código Java
- Não cria novos arquivos além de atualizar os existentes

---

## 4. FILE CONTRACT

```
MODIFY  README.md
        Atualizar seção de estrutura de diretórios:
        - Remover docs/ e seus subdiretórios do diagrama
        - Adicionar steering/, specs/, plans/, results/, contracts/, adr/
        - Atualizar descrições de cada diretório
        - Atualizar qualquer referência a docs/* no corpo do texto

MODIFY  CLAUDE.md
        Atualizar referências de paths:
        - docs/stories/ -> plans/
        - docs/specs/ -> specs/
        - docs/adr/ -> adr/
        - docs/audits/ -> results/audits/
        - Adicionar seção ou atualizar seção existente sobre estrutura SDD

DELETE  docs/                (diretório inteiro — deve estar vazio neste ponto)
        Inclui docs/.DS_Store se existir
```

---

## 5. ACCEPTANCE SCENARIOS

```gherkin
Feature: Atualização de documentação raiz e limpeza de docs/

  Scenario: README.md reflete a nova estrutura de diretórios
    Given README.md existe com referências a docs/
    When  a story STORY-0018-007 é executada
    Then  README.md contém um diagrama de diretórios com:
      | steering/   | Contexto persistente do projeto       |
      | specs/      | Especificações de input humano        |
      | plans/      | Artefatos SDD (epics, stories, maps)  |
      | results/    | Outputs de execução                   |
      | contracts/  | Contratos de API e schemas            |
      | adr/        | Architecture Decision Records         |
    And   README.md não contém referências a "docs/" como diretório

  Scenario: CLAUDE.md reflete novos paths
    Given CLAUDE.md existe com referências a docs/stories/ e docs/specs/
    When  a story STORY-0018-007 é executada
    Then  CLAUDE.md referencia plans/ no lugar de docs/stories/
    And   CLAUDE.md referencia specs/ no lugar de docs/specs/
    And   CLAUDE.md referencia adr/ no lugar de docs/adr/
    And   CLAUDE.md funciona corretamente como project instructions

  Scenario: docs/ removido completamente
    Given docs/ existe mas está vazio (apenas .DS_Store possivelmente)
    When  removemos docs/
    Then  o diretório docs/ não existe no repositório
    And   "git status" não mostra docs/ como tracked

  Scenario: docs/ não está vazio — bloqueio
    Given docs/ ainda contém arquivos não migrados
    When  tentamos remover docs/
    Then  a operação é BLOQUEADA
    And   o agente lista os arquivos restantes
    And   reporta quais stories falharam na migração

  Scenario: CLAUDE.md permanece funcional (RULE-003)
    Given CLAUDE.md foi atualizado
    When  Claude Code carrega o projeto
    Then  as project instructions são carregadas sem erro
    And   as referências a skills e rules permanecem válidas
```

---

## 6. SUB-TASKS

```
[ ] [Dev] TASK-001 — Verificar que docs/ está vazio (apenas .DS_Store permitido)
                      Se não estiver vazio: BLOQUEAR e listar arquivos restantes
[ ] [Dev] TASK-002 — Atualizar README.md: diagrama de diretórios + referências de path
[ ] [Dev] TASK-003 — Atualizar CLAUDE.md: referências de path + descrição SDD
[ ] [Dev] TASK-004 — rm -rf docs/ (incluindo .DS_Store)
[ ] [Dev] TASK-005 — git add -A docs/ (registrar remoção no git)
[ ] [Dev] TASK-006 — Grep por "docs/" em README.md e CLAUDE.md — deve retornar zero
                      (exceto referências genéricas como "documentation" ou "docs for X")
[ ] [Doc] TASK-007 — Atualizar CHANGELOG.md com entrada para EPIC-0018
```

---

## 7. CORRECT vs INCORRECT

### CORRETO

```markdown
<!-- README.md — ANTES: -->
## Project Structure
```
ia-dev-environment/
├── docs/
│   ├── specs/
│   ├── stories/
│   └── adr/
```

<!-- README.md — DEPOIS: -->
## Project Structure
```
ia-dev-environment/
├── steering/        # Persistent project context (product, tech stack, architecture)
├── specs/           # Human-authored specifications and templates
├── plans/           # SDD artifacts (epics, stories, implementation maps)
├── results/         # Execution outputs (audits, reviews, test runs, runbooks)
├── contracts/       # API contracts and schemas (OpenAPI, gRPC, events)
├── adr/             # Architecture Decision Records
```
```

### INCORRETO

```bash
# NÃO remover docs/ se ainda contiver arquivos
rm -rf docs/   # ERRADO se docs/ não está vazio — verificar primeiro

# NÃO atualizar AGENTS.md — é gerado
sed -i 's/docs/plans/g' AGENTS.md   # ERRADO — será regenerado pelo CLI

# NÃO remover referências a docs/ em regras ou skills do .claude/
# Essas são geradas pelo CLI e serão atualizadas após STORY-006 + regeneração
```
