# História: Migração Legado + CHANGELOG + Docs

**ID:** story-0055-0012  
**Chave Jira:** —  
**Status:** Pendente

## 1. Dependências

| Blocked By | Blocks |
| :--- | :--- |
| story-0055-0011 | — |

## 2. Regras Transversais Aplicáveis

| ID | Título |
| :--- | :--- |
| Rule 19 | Backward Compatibility (fallback matrix) |
| Rule 05 | Quality Gates (documentation tests) |

## 3. Descrição

Finalizar épico com:

1. **Migração de épicos legados** — Scan `plans/epic-*/execution-state.json` pré-EPIC-0055. Adicionar `taskTracking: { enabled: false }` a qualquer execution-state sem o field (compatibilidade).

2. **CHANGELOG aggregated** — Entrada para `[Unreleased]` section:
```
## [Unreleased]

### Added

- **EPIC-0055:** Task hierarchy & phase gate enforcement. Introduces Rule 25, skill `x-internal-phase-gate`, and 4-layer enforcement. Operadores agora conseguem ver 4 níveis de granularidade de tasks (Epic › Story › Phase › Wave/Cycle). Phase gates bloqueiam transições de fase até evidências completarem. Backward compatible com epics legados via Rule 19 (campo `taskTracking.enabled`, default false). [story-0055-0001 a 0055-0012]
```

3. **CLAUDE.md atualização** — Bloco "Concluded — EPIC-0055":
```markdown
> **Concluded — EPIC-0055 (Task Hierarchy & Phase Gates).**
> Introduces Rule 25 (hierarchical task tracking), skill `x-internal-phase-gate` (4 modes: pre/post/wave/final), and 4-layer enforcement (normative, Stop hook, PreToolUse hook, CI audit). Operadores conseguem ver "EPIC-0061 › Phase 3 › story › Phase 1 › Arch plan (in_progress)" durante execução. Phase gates bloqueiam transições até artefatos completarem. Backward compatible via Rule 19: campo `taskTracking.enabled` (default false). Stories 0055-0003 a 0055-0010 retrofit 8 orquestradores; story 0055-0011 valida e2e; story 0055-0012 completa migration.
> - Rule: `.claude/rules/25-task-hierarchy.md`
> - Skill: `/x-internal-phase-gate` (internal, haiku)
> - ADR: `adr/ADR-0013-task-hierarchy-and-phase-gates.md`
> - CI scripts: `audit-task-hierarchy.sh`, `audit-phase-gates.sh`, hooks
```

4. **Rule 19 fallback matrix extension** — Adicionar entry em `.claude/rules/19-backward-compatibility.md`:
```markdown
### Task Tracking Field

| Condição em `taskTracking` | Comportamento | Warning? |
| :--- | :--- | :--- |
| Campo ausente | `enabled=false` (legacy — epics pré-EPIC-0055) | **Sim** — visible warning once per run |
| `enabled=true` | Full tracking ligado (Rule 25 gates executadas) | Não |
| `enabled=false` | Tracking pulado; gates não executados (usa Rule 05 + Rule 24 apenas) | Não |
```

5. **Baseline imutável** — `audits/task-hierarchy-baseline.txt` vazio após todas as 8 stories retrofit (ou com entries se skill não foi retrofitada, com comentário explicando).

6. **Documentação de `--legacy-tracking` flag** — Adicionar à seção de flags de orquestradores:
```
--legacy-tracking    Desativa task hierarchy tracking mesmo para novos épicos.
                    Usado durante rollout se bug crítico emerge.
```

## 3.5 Entrega de Valor

- **Valor Principal:** Epic 0055 está completo e documentado. Epics legados continuam funcionando. Baseline permite auditoria gradual.
- **Métrica de Sucesso:** `git log --oneline develop..epic/0055 | wc -l` ≥ 90 (12 stories, cada um multiple commits).
- **Impacto no Negócio:** Rollout de EPIC-0055 não quebra epics em flight. Operadores conseguem optar por legacy mode se necessário.

## 4. DoD Local

- [ ] Migração de 3+ epics legados com task-tracking field adicionado
- [ ] CHANGELOG entry completa
- [ ] CLAUDE.md "Concluded" bloco
- [ ] Rule 19 fallback matrix estendida
- [ ] Baseline vazio ou justificado
- [ ] `--legacy-tracking` flag documentada
- [ ] Zero breaking changes em epics pré-EPIC-0055

## 7. Subtarefas

### Subtarefa [Dev] — Scan & migrate epics

- Find `plans/epic-*/execution-state.json`
- Add `taskTracking` field if absent

### Subtarefa [Doc] — CHANGELOG aggregated entry

- Follow Keep-a-Changelog format
- Include all 12 story references

### Subtarefa [Doc] — CLAUDE.md "Concluded" block

- Summarize Rule 25 + skill + 4-layer enforcement
- Link to Rule 25, ADR-0013, CI scripts

### Subtarefa [Doc] — Rule 19 extension

- Add `taskTracking` field fallback table
- Reference backward-compatibility window

### Subtarefa [Doc] — --legacy-tracking flag docs

- Document in orchestrator flags section
- Explain use case (bug workaround)

### Subtarefa [QA] — Baseline audit

- Verify baseline has 0 entries (all 8 retrofitted)
- Or document each remaining entry with reason

---

**Data de criação:** 2026-04-23  
**Estimativa:** 4–6 horas (documentation + migration)
