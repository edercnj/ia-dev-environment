# História: Integration Smoke Test Epic0055HierarchySmokeTest

**ID:** story-0055-0011  
**Chave Jira:** —  
**Status:** Pendente

## 1. Dependências

| Blocked By | Blocks |
| :--- | :--- |
| story-0055-0010 | story-0055-0012 |

## 2. Regras Transversais Aplicáveis

| ID | Título |
| :--- | :--- |
| Rule 05 | Quality Gates (≥95% / ≥90%) |
| Rule 24 | Execution Integrity (4-layer enforcement) |

## 3. Descrição

Criar `dev.iadev.smoke.Epic0055HierarchySmokeTest` — acceptance test que exercita `x-epic-implement` em fixture real com 2 stories × 3 tasks cada, validando:

1. **Task hierarquia** — 4 níveis renderizados corretamente
2. **Phase gates** — PRE/POST gates passam para todas as phases
3. **Evidências** — Todos os artefatos em disco após execution
4. **CI audit** — `scripts/audit-task-hierarchy.sh` e `audit-phase-gates.sh` passam com zero violations
5. **Performance** — Overhead < 2% vs baseline (medido via telemetry)
6. **Backward compat** — Legacy epics com `taskTracking.enabled=false` continuam funcionando

### 3.1 Fixture

- 2 stories (story-EPIC-0060-0001, story-EPIC-0060-0002)
- 3 tasks por story
- Dependency: story-2 bloqueada por story-1
- Padrão: simples o bastante para rodar em < 10 minutos

### 3.2 Validações

Após execução completa:

```bash
# Validate task list shape
validate-task-list-hierarchy()
  assert 4-level depth
  assert "EPIC-0060 › Phase N › story › Phase › desc" pattern

# Validate gate results
validate-phase-gates()
  assert all gates passed=true
  assert zero missingArtifacts

# Validate artifacts
validate-artifacts()
  assert verify-envelope exists
  assert review stories exist
  assert completion reports exist

# Validate audits
validate-ci-audits()
  audit-task-hierarchy.sh exit 0
  audit-phase-gates.sh exit 0

# Validate performance
validate-performance()
  telemetry overhead < 2%
```

### 3.3 Legacy Mode Test

Subtest com `--legacy-tracking`:
- `taskTracking.enabled=false`
- Gates não executados (skipped)
- Execution ainda succeeds
- Audit treats as baseline

## 3.5 Entrega de Valor

Prove que toda a infraestrutura (Rule 25, gates, audits, hooks) funciona end-to-end com epics reais.

## 4. DoD Local

- [ ] Fixture 2 stories × 3 tasks
- [ ] Test class com 6+ test methods
- [ ] Cobertura ≥ 95%
- [ ] Legacy mode test included
- [ ] Telemetry validation included

## 7. Subtarefas

### Subtarefa [Test] — Fixture setup (2-story EPIC-0060)

### Subtarefa [Test] — 4-level hierarchy validation

### Subtarefa [Test] — Phase gate validation

### Subtarefa [Test] — Artifact validation

### Subtarefa [Test] — CI audit integration

### Subtarefa [Test] — Performance baseline capture

### Subtarefa [Test] — Legacy mode subtest

---

**Data de criação:** 2026-04-23  
**Estimativa:** 10–14 horas (test harness setup, fixture, 6+ test methods)
