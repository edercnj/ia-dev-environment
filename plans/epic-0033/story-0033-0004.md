# STORY-0033-0004: Consolidate Naming — x-dev-lifecycle to x-dev-story-implement

## Status: VOID

> **Why VOID:** This story's acceptance criteria were fully satisfied by **EPIC-0032 — Skill Naming Standardization** (merged via PR #255) before STORY-0033-0004 implementation began. Specifically:
>
> - Commit `7a5305377` (test(story-0032-0007): regenerate golden files for 17 profiles after skill rename) deleted `core/x-dev-lifecycle/SKILL.md` entirely (~820 lines removed) and consolidated all references to `x-dev-story-implement`.
> - Commit `6a0e9ad9f` (merge of PR #255) brought the rename into `develop`.
> - Commit `b5b1adbad` (chore(epic-0032): regenerate .claude/ outputs) materialized the corresponding `.claude/` regeneration into the repo (PR #256, merged as `e11307d17`).
>
> **Acceptance criteria status (at time of reconciliation):**
> - AC-1 (x-dev-epic-implement references x-dev-story-implement): ✅ satisfied by EPIC-0032.
> - AC-2 (content merged into x-dev-story-implement): ✅ satisfied — x-dev-lifecycle was removed entirely; `x-dev-story-implement` is now the canonical name.
> - AC-3 (x-dev-lifecycle removed): ✅ satisfied — directory removed.
> - AC-4 (no stale references): ✅ verified via `grep -r "x-dev-lifecycle" java/src/main/resources/targets/` → 0 matches.
> - AC-5 (reference files consolidated): ✅ satisfied — `x-dev-story-implement/references/` now contains `planning-phases.md`, `verification-phase.md`, `scope-assessment.md`, and `openapi-generator.md`.
> - AC-6 (golden files updated): ✅ satisfied by EPIC-0032 story 0032-0007.
>
> **This file is retained for historical traceability.** No implementation work is required. The original content below is frozen as-is.
>
> ---

## Parent Epic: EPIC-0033

## Depends On: — (no dependencies, Phase 1)

## Summary

Consolidate the duplicate skill naming by making `x-dev-story-implement` the canonical name. Remove `x-dev-lifecycle` as a standalone skill and update all references in `x-dev-epic-implement` and other skills that point to the old name.

## User Story

As a **developer or skill author**, I want a single canonical name for the story implementation lifecycle skill, so that there is no confusion about which skill to invoke and no risk of invoking a stale version.

## Background

Two nearly identical skills exist:
- `core/x-dev-lifecycle/SKILL.md` — The older name, referenced by x-dev-epic-implement's subagent prompt
- `core/x-dev-story-implement/SKILL.md` — Newer version with `AskUserQuestion` in allowed-tools

The x-dev-epic-implement subagent prompt tells the subagent:
```
Skill(skill: "x-dev-lifecycle", args: "{storyId}")
```

But `x-dev-story-implement` is the more descriptive name and the more up-to-date version. This story consolidates to `x-dev-story-implement`.

## Acceptance Criteria

### AC-1: x-dev-epic-implement references x-dev-story-implement

```gherkin
DADO o template SKILL.md do x-dev-epic-implement com o prompt do subagente
QUANDO a instrução de dispatch do subagente é gerada
ENTÃO ela DEVE referenciar "x-dev-story-implement" e não "x-dev-lifecycle":
  Skill(skill: "x-dev-story-implement", args: "{storyId}")
E a instrução de fallback DEVE também referenciar o nome correto
```

### AC-2: x-dev-lifecycle content merged into x-dev-story-implement

```gherkin
DADO os arquivos SKILL.md de x-dev-lifecycle e x-dev-story-implement
QUANDO comparamos seu conteúdo
ENTÃO x-dev-story-implement DEVE conter todas as features de ambas as versões
E x-dev-story-implement DEVE ter "AskUserQuestion" nos allowed-tools (da versão mais nova)
E x-dev-story-implement DEVE ter todos os arquivos de referência de x-dev-lifecycle
```

### AC-3: x-dev-lifecycle removed or deprecated

```gherkin
DADO que a consolidação está completa
QUANDO eu busco pelo diretório x-dev-lifecycle
ENTÃO uma das opções:
  a) O diretório é removido completamente (preferido)
  b) O SKILL.md contém apenas um redirect: "This skill has been renamed to x-dev-story-implement."
```

### AC-4: No stale references remain

```gherkin
DADO todos os arquivos SKILL.md em core/
QUANDO eu busco pela string "x-dev-lifecycle"
ENTÃO ZERO matches são encontrados em instruções de delegação
E ZERO matches em allowed-tools ou tabelas de referência de skills
E os únicos matches aceitáveis são em notas de changelog/migração
```

### AC-5: Reference files consolidated

```gherkin
DADO que x-dev-lifecycle tem references/:
  - planning-phases.md
  - verification-phase.md
  - scope-assessment.md
  - openapi-generator.md
E x-dev-story-implement tem references/:
  - planning-phases.md
  - verification-phase.md
  - scope-assessment.md
  - openapi-generator.md
QUANDO consolidados
ENTÃO x-dev-story-implement/references/ DEVE conter a versão mais atualizada de cada arquivo
E x-dev-lifecycle/references/ é removido (se o diretório for removido)
```

### AC-6: Golden files updated

```gherkin
DADO a mudança de nomenclatura
QUANDO golden files são regenerados para todos os perfis suportados
ENTÃO golden files NÃO DEVEM conter "x-dev-lifecycle" em referências de skills
E golden files DEVEM referenciar "x-dev-story-implement" onde aplicável
```

## Affected Files

| File | Change |
|------|--------|
| `src/main/resources/targets/claude/skills/core/x-dev-epic-implement/SKILL.md` | Update subagent prompt: `x-dev-lifecycle` -> `x-dev-story-implement` (line 284-285) |
| `src/main/resources/targets/claude/skills/core/x-dev-story-implement/SKILL.md` | Ensure all content from x-dev-lifecycle is present |
| `src/main/resources/targets/claude/skills/core/x-dev-story-implement/references/*` | Ensure all reference files are up-to-date |
| `src/main/resources/targets/claude/skills/core/x-dev-lifecycle/` | Remove directory or add deprecation redirect |
| `src/main/resources/targets/claude/skills/core/x-tdd/SKILL.md` | Update integration table reference (line 455) |
| `src/test/resources/golden/*/` | Regenerate all affected golden files |

## Sub-Tasks

- [ ] T-001: Diff x-dev-lifecycle and x-dev-story-implement SKILL.md to identify differences
- [ ] T-002: Merge any missing content from x-dev-lifecycle into x-dev-story-implement
- [ ] T-003: Merge reference files (take most up-to-date version of each)
- [ ] T-004: Update x-dev-epic-implement subagent prompt to reference x-dev-story-implement
- [ ] T-005: Update x-tdd integration table reference
- [ ] T-006: Search all skills for "x-dev-lifecycle" and update references
- [ ] T-007: Remove or deprecate x-dev-lifecycle directory
- [ ] T-008: Regenerate golden files
- [ ] T-009: Run tests

## Risks

| Risk | Mitigation |
|------|------------|
| Breaking existing skill invocations | Search all SKILL.md files for "x-dev-lifecycle" before removing |
| Golden file diffs may be large | Expected: directory rename propagates to all profiles |
| x-dev-lifecycle may have unique content | Diff first (T-001), merge differences before removing |

## Estimation

- Size: M (Medium)
- Complexity: Low — Primarily rename/consolidation with careful reference updates
