# História: Atualizar Java Source Code

**ID:** story-0032-0005
**Chave Jira:** —
**Status:** Pendente

## 1. Dependências

| Blocked By | Blocks |
| :--- | :--- |
| story-0032-0004 | story-0032-0006 |

## 2. Regras Transversais Aplicáveis

| ID | Título |
| :--- | :--- |
| RULE-003 | Completude de Rename |

## 3. Descrição

Como **desenvolvedor**, eu quero que o código Java do gerador (assemblers e selection) use os novos nomes para que os outputs gerados tenham os nomes corretos.

### 3.1 SkillGroupRegistry.java

Arquivo: `java/src/main/java/dev/iadev/application/assembler/SkillGroupRegistry.java`

Mapeamentos de grupo para skills — atualizar strings dos 25 nomes renomeados:
- Grupo `story`: `"x-plan-task"` → `"x-task-plan"`
- Grupo `dev`: `"x-dev-lifecycle"` → `"x-dev-story-implement"`, `"x-docs"` → `"x-doc-generate"`, `"x-commit"` → `"x-git-commit"`, `"x-format"` → `"x-code-format"`, `"x-setup-dev-environment"` → `"x-setup-env"`, `"x-ci-cd-generate"` → `"x-ci-generate"`, `"x-lint"` → `"x-code-lint"`, `"x-worktree"` → `"x-git-worktree"`
- Grupo `review`: `"x-codebase-audit"` → `"x-code-audit"`, `"x-spec-drift-check"` → `"x-spec-drift"`, `"x-contract-lint"` → `"x-test-contract-lint"`
- Grupo `testing`: `"x-tdd"` → `"x-test-tdd"`
- Grupo `infrastructure`: `"setup-environment"` → `"x-setup-stack"`
- Grupo `git-troubleshooting`: `"x-fix-pr-comments"` → `"x-pr-fix-comments"`, `"x-fix-epic-pr-comments"` → `"x-pr-fix-epic-comments"`, `"x-changelog"` → `"x-release-changelog"`
- `INFRA_SKILL_CONDITIONS`: `"setup-environment"` → `"x-setup-stack"`
- `REVIEW_SKILL_CONDITIONS`: `"x-contract-lint"` → `"x-test-contract-lint"`

### 3.2 SkillsSelection.java

Arquivo: `java/src/main/java/dev/iadev/application/assembler/SkillsSelection.java`

Atualizar strings em métodos de seleção condicional:
- `selectInterfaceSkills`: `"x-contract-lint"` → `"x-test-contract-lint"`
- `selectInfraSkills`: `"instrument-otel"` → `"x-obs-instrument"`, `"setup-environment"` → `"x-setup-stack"`
- `selectSecurityScanningSkills`: `"x-sast-scan"` → `"x-security-sast"`, `"x-dast-scan"` → `"x-security-dast"`, `"x-secret-scan"` → `"x-security-secret-scan"`, `"x-container-scan"` → `"x-security-container"`, `"x-infra-scan"` → `"x-security-infra"`, `"x-sonar-gate"` → `"x-security-sonar"`
- `selectPentestSkills`: `"x-pentest"` → `"x-security-pentest"`

### 3.3 Entrega de Valor

- **Valor Principal:** Gerador produz outputs com nomes padronizados
- **Métrica de Sucesso:** Zero referências a nomes antigos em main/java/
- **Impacto no Negócio:** Skills gerados em `.claude/` terão os novos nomes

## 4. Definições de Qualidade Locais

### DoR Local (Definition of Ready)

- [ ] Story 0032-0004 concluída (referências cruzadas nos .md atualizadas)

### DoD Local (Definition of Done)

- [ ] SkillGroupRegistry.java — todas as strings atualizadas
- [ ] SkillsSelection.java — todas as strings atualizadas
- [ ] `grep -r '"x-dev-lifecycle"' java/src/main/java/` retorna zero
- [ ] Build compila sem erros (`mvn compile`)

### Global DoD

- **Cobertura:** ≥ 95% Line, ≥ 90% Branch
- **Testes Automatizados:** Compilação OK (testes atualizados em story-0032-0006)
- **Relatório de Cobertura:** JaCoCo
- **Documentação:** N/A
- **Persistência:** N/A
- **Performance:** N/A

## 5. Contratos de Dados (Data Contract)

### 5.1 Arquivos Modificados

| Arquivo | Descrição |
| :--- | :--- |
| `java/src/main/java/dev/iadev/application/assembler/SkillGroupRegistry.java` | Mapeamentos de grupo |
| `java/src/main/java/dev/iadev/application/assembler/SkillsSelection.java` | Seleção condicional |

## 6. Diagramas

N/A.

## 7. Critérios de Aceite (Gherkin)

```gherkin
@GK-1
Cenário: SkillGroupRegistry sem nomes antigos
  DADO o arquivo SkillGroupRegistry.java
  QUANDO busco por cada um dos 25 nomes antigos entre aspas
  ENTÃO nenhum é encontrado

@GK-2
Cenário: SkillsSelection sem nomes antigos
  DADO o arquivo SkillsSelection.java
  QUANDO busco por cada um dos 25 nomes antigos entre aspas
  ENTÃO nenhum é encontrado

@GK-3
Cenário: Build compila
  DADO as alterações em ambos os arquivos
  QUANDO executo mvn compile
  ENTÃO a compilação é bem-sucedida
```

## 8. Tasks

### TASK-0032-0005-001: Atualizar SkillGroupRegistry.java

- **Layer:** Application
- **Test Type:** Unit
- **Size:** M
- **Dependencies:** —
- **Branch:** `feat/task-0032-0005-001-skill-group-registry`
- **Testability:** `mvn compile` + grep para nomes antigos
- **Files:**
  - `java/src/main/java/dev/iadev/application/assembler/SkillGroupRegistry.java`
- **Acceptance Criteria:**
  - [ ] Todas as strings de nomes antigos substituídas
  - [ ] Compilação sem erros

### TASK-0032-0005-002: Atualizar SkillsSelection.java

- **Layer:** Application
- **Test Type:** Unit
- **Size:** S
- **Dependencies:** —
- **Branch:** `feat/task-0032-0005-002-skills-selection`
- **Testability:** `mvn compile` + grep para nomes antigos
- **Files:**
  - `java/src/main/java/dev/iadev/application/assembler/SkillsSelection.java`
- **Acceptance Criteria:**
  - [ ] Todas as strings de nomes antigos substituídas
  - [ ] Compilação sem erros
